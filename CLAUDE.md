# CLAUDE.md

Behavioral guidelines to reduce common LLM coding mistakes. Merge with project-specific instructions as needed.

**Tradeoff:** These guidelines bias toward caution over speed. For trivial tasks, use judgment.

## 1. Think Before Coding

**Don't assume. Don't hide confusion. Surface tradeoffs.**

Before implementing:
- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them - don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.
- If something is unclear, stop. Name what's confusing. Ask.

## 2. Simplicity First

**Minimum code that solves the problem. Nothing speculative.**

- No features beyond what was asked.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested.
- No error handling for impossible scenarios.
- If you write 200 lines and it could be 50, rewrite it.

Ask yourself: "Would a senior engineer say this is overcomplicated?" If yes, simplify.

## 3. Surgical Changes

**Touch only what you must. Clean up only your own mess.**

When editing existing code:
- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- Match existing style, even if you'd do it differently.
- If you notice unrelated dead code, mention it - don't delete it.

When your changes create orphans:
- Remove imports/variables/functions that YOUR changes made unused.
- Don't remove pre-existing dead code unless asked.

The test: Every changed line should trace directly to the user's request.

## 4. Goal-Driven Execution

**Define success criteria. Loop until verified.**

Transform tasks into verifiable goals:
- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "Write a test that reproduces it, then make it pass"
- "Refactor X" → "Ensure tests pass before and after"

For multi-step tasks, state a brief plan:
```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

Strong success criteria let you loop independently. Weak criteria ("make it work") require constant clarification.

---

**These guidelines are working if:** fewer unnecessary changes in diffs, fewer rewrites due to overcomplication, and clarifying questions come before implementation rather than after mistakes.

---

# Project: Freeman Plugin Suite

The guidelines above apply universally. The rules below are specific to this repo and override defaults where they conflict.

## Repo state

- Path: `/Users/freemansmain/Ai Projects/Freeman Theme/`
- Packages: `freeman-core` (v1.21.38), `freeman-digital` (v1.7.4), `freeman-theme` (v1.11.27)
- Audit: `/docs/audit-2026-04-28.md`
- Decisions: `/docs/decisions-2026-04-28.md` — read this before any roadmap work
- Roadmap: `/docs/roadmap.md`
- PR template: `/docs/pr-template.md`

## Current infrastructure state

*Last verified: 2026-07-05 (PR-16 / 1.21.38).*

- **PHPUnit**: configured for freeman-core (root `phpunit.xml.dist`, `tests/bootstrap.php`, **439 tests / 1499 assertions**, all green; Wave 3.2a (1.11.29) added 8 reported tests, +8 reported total; Wave 3.2b (1.11.30) added 10 reported tests, +37 reported assertions; Wave 3.3 (1.11.32) added 9 reported tests, +10 reported assertions; Wave 3.1a (1.11.33) added 8 reported tests, +23 reported assertions; Wave 3.1b (1.11.35) added 10 reported tests, +17 reported assertions; Wave 4.3 (1.11.36) added 6 reported tests, +35 reported assertions; Wave 4.1a (1.11.37) added 11 reported tests, +32 reported assertions; Wave 4.1b (1.11.38) added 10 reported tests, +16 reported assertions; Wave 4.2 (1.11.39) added 10 reported tests, +27 reported assertions; Wave 4.5 (1.11.40) added 4 reported tests, +8 reported assertions; ProductSlider popularity/rating/price orderby bugfix (1.11.42) added 9 reported tests, +18 reported assertions; Wave 5.1 (1.11.44) added 8 reported tests, +239 reported assertions — the assertion jump is loop-heavy: several `FeatureFlagsAdminTest` cases iterate the flag registry; Wave 2.2 / 4g (1.11.45) net +7 reported tests, +0 reported assertions — added `VariationSwatchesSettingsRelocationTest`, rewrote `VariationSwatchesSettingsReaderTest` (dropped the now-defunct settings_hub flag-OFF/ON split), deleted `VariationSwatchesSettingsHubSnapshotTest`; bug fix 1.11.50 added 3 reported tests, +3 reported assertions; Shop Filters Wave 6 / 6.1 (1.12.0) added 14 reported tests, +66 reported assertions; Shop Filters Wave 6 / 6.2 (1.12.1) added 16 reported tests, +50 reported assertions; Shop Filters 6.1 indexer bug-fix (1.12.2) added 1 reported test, +4 reported assertions; Shop Filters admin toggle/status surface (1.12.3) added 1 reported test, +3 reported assertions; Shop Filters reconcile-scheduling init-timing fix (1.12.4) added 0 reported tests, +1 reported assertion; Shop Filters 6.3a frontend read path (1.12.6) added 10 reported tests, +48 reported assertions; Shop Filters 6.3a.1 read-path fix — query bridge + reload transport (1.12.7) added 5 reported tests, +16 reported assertions; Shop Filters 6.3a.2 stock-visibility parity (1.12.8) added 0 reported tests, +0 reported assertions — $wpdb/WC integration fix, live-QA verified; Shop Filters 6.3a.3 index diagnostic (1.12.9) added 4 reported tests, +13 reported assertions; Shop Filters 6.3a.4 hyphen-taxonomy + pagination fix (1.12.10) added 1 reported test, +3 reported assertions; Shop Filters 6.3b Facet UI (1.12.11) added 3 reported tests, +15 reported assertions — pure seams: `shape_category_nodes` + swatch enrichment/type-flip in `shape_facets` (the category-tree wiring, swatch render, and mobile drawer are live-QA); Shop Filters 6.3c string-labels editor (1.12.12) added 7 reported tests, +65 reported assertions — `ShopFiltersLabelsTest` (defaults map, option override + blank fallback, singular/plural count line); Shop Filters 6.5b price + sort (1.12.13 — built ahead of 6.4/6.5a, took the next sequential version) added 12 reported tests, +37 reported assertions — `ShopFiltersPriceTest` (Url_State price-band parse/serialize, band construction + nice_round, count/filter/shape seams, `Query::price_where_sql`); Shop Filters in-stock-filtering bug-fix (1.12.14) added 4 reported tests, +4 reported assertions — `Query::intersect_id_sets` (AND-across/OR-within for the index-driven `post__in`); the grid + counts now use the index's per-variation in-stock truth so a sold-out size drops out (was matching WC parent-assigned terms); Shop Filters search faceting (1.12.17) added 0 reported tests (integration: WP product search base + `Index_Repository::filter_indexed`, live-QA); Shop Filters AWS search-grid enforcement (1.12.18) added 2 reported tests, +4 reported assertions — `Query::filter_posts_to_ids` (the `the_posts` safety net that drops products outside the in-stock filtered set when a search plugin like Advanced Woo Search supplies the search grid); Shop Filters 6.4 admin facet config (1.12.21) added 6 reported tests, +34 reported assertions — `ShopFiltersFacetConfigAdminTest` (`Facet_Config::sanitize` matrix normalisation + unknown-taxonomy drop, `all_defs` keeps disabled facets for the editor, and the `admin_config`-flag gate on `saved()` so flag-off reverts to auto-derived defaults); Shop Filters 6.5a filtered-URL SEO policy (1.12.22) added 15 reported tests, +42 reported assertions — `ShopFiltersSeoTest` (`Seo::is_filtered_state` true for facet/price/stock selections + false for sort-only/page-only/empty, `clean_url` strips filter/sort/pagination + `/page/N/` while keeping unrelated params/port and is idempotent, and the `noindex_index_follow` / `noindex_core` robots-array mutations); the test jump is dataProvider-driven (the two `is_filtered_state` cases iterate 6 + 3 rows); Shop Filters 6.5c on-sale/in-stock facets (1.12.23) added 12 reported tests, +34 reported assertions — `ShopFiltersFlagsTest` (`Query::flags_where_sql` on-sale/in-stock/both/neither + alias sanitisation, `Query_Builder::filter_by_flags` AND-across intersect, `shape_flag_facet` hide-zero + keep-selected + in-stock-only-when-OOS-shown); RestockNotify privacy-when-disabled fix (1.12.24) added 1 reported test, +2 reported assertions — `RestockNotifyPrivacyTest::test_persistent_hooks_register_privacy_even_when_module_disabled` (the new `Module_Base::register_persistent_hooks()` seam attaches the WP privacy export/erase filters regardless of module-enabled state); Shop Filters non-Latin slug blank-grid fix (1.12.25) added 1 reported test, +2 reported assertions — `ShopFiltersUrlStateTest::test_preserves_percent_encoded_nonlatin_slug` (`Url_State::sanitize_slug()` now preserves `%` so WordPress's percent-encoded Hebrew term slugs survive parse + serialize; without it `get_term_by('slug',…)` matched nothing and the storefront query forced `post__in=[0]` → blank grid); Shop Filters graduation + Index diagnostic removal (1.12.26) net -9 reported tests, -112 reported assertions — hard-removed all four `shop_filters` feature flags (`indexer`/`frontend`/`seo_policy`/`admin_config`) and their registry entries so the module is always-on (no kill-switch — owner-approved override of hard rules 1/2); also removed the now-dead `indexer_enabled` settings toggle, deleted `Diagnostics`/`ShopFiltersDiagnosticsTest`, and dropped the flag-gated test cases in `ShopFiltersModuleTest`/`ShopFiltersFrontendTest`/`ShopFiltersIndexerTest`/`ShopFiltersFacetConfigAdminTest`; Wave 7.1a ProductSlider grid-mode mobile-columns fix (1.12.31) added 3 reported tests, +3 reported assertions — `ProductSliderGridMobileColumnsTest` (grid mode rounds fractional `per_view_mobile` to a whole `repeat()` count; slider keeps the float peek); Wave 7.2 QuickView module (1.13.0) added 17 reported tests, +89 reported assertions — `QuickViewModuleTest` / `QuickViewFrontendTest` / `QuickViewAjaxTest` (flag-off zero-hooks contract, trigger/drawer-shell markup seams, footer-shell gating, show_trigger filter, localize payload, AJAX wiring + `is_viewable` guard; the assertion jump includes registry-iterating tests scaling with the new flag + module); QuickView icon/drawer specificity fixes (1.14.1) added 0 reported tests — CSS + a non-asserted SVG glyph swap, live-QA on the Elementor store; ProductSlider Elementor-archive grid-cap fix (1.14.2) added 9 reported tests, +9 reported assertions — `ProductSliderArchiveQueryTest` (`main_query()` prefers `$wp_the_query` over a swapped `$wp_query`, `is_product_archive()` detects via the query object's predicates as well as the conditional tags, `collect_archive_products()` grid-uncapped / slider-capped / drops non-product entries; the real archive-detection against a live Elementor render is store QA); ProductSlider grid polish (1.14.3) added 2 reported tests, +3 reported assertions — `ProductSliderHeadTest` (`.cs-head` is omitted when it has no eyebrow/headline/arrows and present when a headline is set; the grid clearfix-pseudo-element removal and InfiniteScroll skeleton-height match are CSS, live-QA); HoverSwap module (1.15.0) added 11 reported tests, +44 reported assertions — `HoverSwapModuleTest` / `HoverSwapFrontendTest` (flag-off zero-hooks contract, the `before_shop_loop_item_title` priority-11 injection, `second_image_id` = first gallery image / 0 when none, `overlay_html` markup + no-gallery no-op, and the `show` filter; the cross-fade CSS + RTL/hover-only/reduced-motion behaviour are live-QA on the Elementor store); Card-image gallery-slider mode (1.16.0) added 12 reported tests, +56 reported assertions — `HoverSwapGallerySliderTest` + extended `HoverSwapModuleTest` (the `card_image_mode` select + slider-toggle schema defaults, boot routing for none/hover_swap/gallery_slider incl. the gallery_slider flag gate, `slide_image_ids` primary-first-deduped, `slider_html` single-image fallback + arrows/dots/autoplay toggles); the scroll-snap slider's swipe/drag/MutationObserver-reinit JS is live-QA on the Elementor store); Card-image dropdown fix + activation simplification (1.16.1) net -3 reported tests, -44 reported assertions — fixed the `card_image_mode` select rendering empty (`Settings_Hub::render_field` reads the `choices` key, not `options`; the schema now matches the InfiniteScroll/ShopFilters convention) and deleted both `hover_swap` feature flags (`frontend`/`gallery_slider`), routing the module purely on the `card_image_mode` setting (default `none`) — owner-approved override of Hard Rule #1/#2, like Shop Filters §5.8; the removed flag-gated `HoverSwapModuleTest` cases + the two registry entries account for the drop. Note: `CheapestDefaultVariation`'s `strategy` select has the same latent `options`-vs-`choices` bug (untouched — flag-surfaced to owner); Card slider autoplay+dots removal (1.16.2) net -2 reported tests, -6 reported assertions — owner reported autoplay buggy and dots unnecessary, so the `slider_autoplay`/`slider_dots` settings, the dots markup/CSS/JS and the autoplay JS are gone; the slider keeps swipe/drag + optional hover arrows (`HoverSwapGallerySliderTest` dots/autoplay cases dropped); 1.17.0 polish wave (ProductSlider price typography control + 1rem card-image radius + subtle resting card border; QuickView wider drawer + smaller locale-aware price + fade/scale open-close + gallery prev/next arrows; VariationSwatches locale-aware buy-box labels) added 9 reported tests, +42 reported assertions — `VariationSwatchesLabelsTest` (He/En `Labels::get()` by site locale) + extended `QuickViewModuleTest` (`gallery_image_ids` featured-first / dedupe / no-featured); 1.17.1 follow-up (QuickView gallery arrows restyled to match the HoverSwap card-slider arrows, both galleries clamp at the ends instead of wrapping, card-slider product images get a 1rem radius on the `.fc-card-slider` container) added 0 reported tests — CSS/JS/template only, live-QA; 1.17.2 follow-up (QuickView drawer gallery now reuses the HoverSwap card-slider component verbatim — same markup/CSS/JS, auto-inited by card-slider.js's MutationObserver; card-slider nav loops with an instant wrap step so it no longer scrolls the whole strip back) added 0 reported tests — CSS/JS/template only, live-QA; 1.17.3 follow-up (card-slider loop wrap is now smooth via cloned end slides — step animates onto a clone then silently snaps to the real twin once scrolling settles; RTL-correct + page-safe) added 0 reported tests — JS only, live-QA; Search Wave 1 foundation (1.18.0) added 14 reported tests, +71 reported assertions — new `Search` module (in-house full-text product search, staged to replace Advanced Woo Search): `SearchDatabaseTest` (`freeman_search_index` schema shape + FULLTEXT/InnoDB clauses), `SearchQueryEngineTest` (pure `build_search_text` blob assembly / strip-tags / whitespace-collapse), `SearchIndexerTest` (dirty-queue mechanics, search-namespaced option), `SearchModuleTest` (flag-off zero-hooks + flag-on indexer wiring); behind `freeman_core_search_indexer_enabled` (default off). Index sync + reconcile sweep + per-product reindex are integration/live-QA. Read path (MATCH...AGAINST score/where + dropdown + results page) deferred to Waves 2–3; Search Wave 8.2 live dropdown (1.19.0) added 8 reported tests, +61 reported assertions — `Query_Engine::search_sql`/`search_args` (the ranked MATCH...AGAINST template + placeholder-arg ordering with the LIKE/exact-SKU fallback for short/non-Latin tokens), `Search_Repository::search` blank-term guard (live ranked query is integration), `Frontend::localized_payload` shape + setting overrides, and `Module` dropdown-flag wiring (`Ajax`+`Frontend` behind `freeman_core_search_dropdown_enabled`, independent of the indexer flag) + the 3 dropdown settings. New `Ajax` public endpoint (`freeman_core_search_query`, nonce + rate-limit, server-rendered price_html), `Frontend` (selector-attached debounced combobox dropdown — `search.js`/`search.css`, progressive enhancement, live-QA); Search 8.2a `[freeman_search]` shortcode (1.19.1) added 2 reported tests, +5 reported assertions — `Frontend::render_form` (a standalone GET product-search box rendering `input[type=search][name=s]` so the default dropdown selector auto-enhances it + `post_type=product` hidden for the JS-off native fallback; honours `placeholder`/`button` atts); added a `get_search_query` bootstrap stub; Search Wave 8.3 results page (1.20.0) added 11 reported tests, +37 reported assertions — `Results_Query` pure seams (`should_handle` gating matrix via dataProvider — front-end product search + has-data only; `plan_ids` no-match→[0] / engine-order preserved; `neutralize_search` inactive pass-through) + `Module` results-flag wiring; the `pre_get_posts` takeover (`post__in`=engine ids + `orderby`=post__in at prio 5), `posts_search` neutralisation, `has_data()` and `supply_engine_ids` (the additive `freeman_core/shop_filters/search_product_ids` facet-feed) are integration/live-QA (`Search_Repository` is final + the read path needs $wpdb). New additive hook `freeman_core/shop_filters/search_product_ids` in ShopFilters Query_Builder (baseline-hooks regenerated). Behind `freeman_core_search_results_enabled` (default off — the pre-cutover staging switch); Search 8.3a live-QA fixes (1.20.1) net 0 reported tests — (a) **results-page blank-grid + empty-facets bug**: the results page and the facet-feed call `search($term, -1)` for "all results", which built `LIMIT -1` (invalid MySQL) → empty → grid forced to `[0]` and facet base empty; `Search_Repository::search()` now clamps `limit <= 0` to an effectively-infinite cap. Surfaced only once AWS was deactivated (AWS had been supplying the search grid). (b) removed the `field_selector` setting (owner uses the `[freeman_search]` shortcode only) — the dropdown selector is now the hardcoded `input[type="search"], input[name="s"]` the shortcode field matches; the two affected tests were re-pointed (selector→numeric overrides; schema no-field_selector); Search Wave 8.4 graduation (1.21.0) net -3 reported tests, -71 reported assertions — the Search module graduated to always-on (owner-approved override of Hard Rule #1/#2, like Shop Filters 6.6, since Advanced Woo Search was deactivated and the engine is live): hard-removed the three `search` feature flags (`indexer`/`dropdown`/`results`) + their registry entries, so `Module::boot()` wires the indexer, dropdown + `[freeman_search]` shortcode + public endpoint, and the engine-driven results page whenever the module is enabled (module-enable toggle = the single kill-switch; a not-yet-built index degrades safely via `has_data()` → dropdown empty + results fall back to native WP search). Replaced the four flag-gated `SearchModuleTest` cases with one `test_boot_wires_all_surfaces`; the assertion drop also reflects `FeatureFlagsAdminTest`'s registry-iterating cases covering 3 fewer flags. The originally-planned 8.4 (parity diff vs AWS + cutover) was obsoleted by AWS already being deactivated. **Wave 8 epic complete.** Search 8.3b results-grid enforcement (1.21.1 → 1.21.2) net +1 reported test — owner live-QA on arba4: the facet panel read N products but the grid showed *all* products, because the search-results grid there is a Freeman **ProductSlider** in current-query mode, which doesn't treat a search as an archive (`is_product_archive()` only matches shop/post-type-archive/taxonomy) and so falls back to source `all` (every product), ignoring the main query entirely. 1.21.1 first tried a `the_posts` priority-99999 net (`order_posts_by_ids`) — but the widget reads its own `wc_get_products()` query, not the constrained main query, so the net filtered the all-products query down to ~0; **reverted**. 1.21.2 hooks the widget's own `freeman_core/product_slider/query_args` filter (`Results_Query::constrain_slider_query`, scoped to `source === 'current_query'`, the search read from `$wp_the_query` to survive Elementor's `$wp_query` render-swap) and injects the engine ids via wc_get_products `include` + `orderby=include` (`[0]` on no-match = empty grid). Also lowered `Indexer::DEBOUNCE_DELAY` 30s → 10s for near-live index freshness on a fast-moving shop (still queued/batched/background, so bulk edits still collapse to one reindex per product). Neither the reverted `the_posts` nor the new `query_args` listener is a hook *declaration*, so baselines are unchanged; Search 8.3c request-term root-cause fix (1.21.3) net -1 reported test — the grid still showed all products + paginated (page 2) because on arba4 the search results page is an **Elementor product-archive** template whose main query carries `post_type=product` but NOT the `s` query var (the term lives only in the URL), so `apply()`'s `is_search()` gate + `$q->get('s')` read both missed it (only ShopFilters, which reads the term from the request, was correct → the facet/grid mismatch). Fix: `Results_Query::apply()` now reads the term from the request (`$_GET['s']`, sanitised) and gates on **product-main-query + request-term + has_data** (is_search dropped), so it constrains the main query — fixing both grid content AND pagination (`max_num_pages` follows the engine count); `constrain_slider_query` reads the request term too. `should_handle()` lost its `is_search` param (dataProvider -1 case). No new hooks/options/strings → baselines + `.pot` unchanged; Search 8.3d archive-grid source fix (1.21.4) added 7 reported tests, +7 reported assertions — `SearchResultsQueryTest::test_should_constrain_slider` (dataProvider matrix); owner live-QA on arba4: the search-results grid is a Freeman **ProductSlider in grid mode** rendered by a non-editable Elementor archive template, configured with the fixed `all` source (not `current_query`), so `constrain_slider_query`'s `=== 'current_query'` guard bailed and the widget rendered the whole catalog (the main-query `post__in` from `apply()` is moot — the widget runs its own `wc_get_products()`). Extracted the guard into the pure `Results_Query::should_constrain_slider($source, $is_grid)` and broadened it to also constrain a **grid-mode `all`** widget (current_query still constrained in any mode; slider-mode `all` carousels + other curated sources left alone). No new hooks/options/strings → baselines + `.pot` unchanged; Search 8.3e archive-grid root-cause fix (1.21.4 source-guess was WRONG — the arba4 grid widget is `current_query`, not `all`; 1.21.5 was a temporary SEARCHDIAG Logger diagnostic build, reverted) (1.21.6) added 4 reported tests, +4 reported assertions — `ProductSliderArchiveQueryTest::test_should_use_archive` (dataProvider matrix). The 1.21.5 diagnostic proved `apply()` *does* set `post__in`=engine-ids on the main query (`handle=1`), but the **ProductSlider widget reads a *different*, unconstrained main query** (`$wp_the_query` at render time has `post__in` empty, `is_product_archive=1`) and so takes the archive sub-path (`collect_archive_products`), returning the whole catalog — the query-level constraint never reaches it. Root-cause fix is in the **widget** (ProductSlider): a product *search* is no longer treated as a plain archive — `Widget::should_use_archive($main_is_search, $is_product_archive)` returns false when the main query `is_search()`, so a search falls through to the `wc_get_products()` path where `constrain_slider_query` (the `query_args` filter) injects the engine ids with `limit=-1`; the `current_query` grid pagination is likewise forced to a single page on a search. Genuine archives (shop/category/tag) still read the main query directly. No new hooks/options/strings → `.pot` unchanged; `baseline-hooks.txt` regenerated for the `Widget.php` `query_args` line shift (888→897, position-only); Search in-stock + UI redesign (1.21.7) net 0 reported tests — flipped `Search_Repository::search(..., true)` at all four call sites (dropdown + results grid + slider + facet feed) so out-of-stock products are hidden everywhere (engine already supported it; the in-stock SQL is already covered by `SearchQueryEngineTest`, the flipped call sites are integration/live-QA), and redesigned the storefront search to an icon that expands a full-width bar below the header (scrim + slide, `prefers-reduced-motion`-gated) with a full-screen mobile takeover + visible Search button — all layered in `search.js`/`search.css` (`render_form()` stays native, no test/baseline drift); Search dropdown display controls + variation-SKU fix (1.21.8) added 2 reported tests, +7 reported assertions — `SearchQueryEngineTest::test_build_search_text_includes_variation_skus` (variable-product variation SKUs now fold into the `search_text` blob via the new `build_search_text($variation_skus)` param, so a variation-SKU search matches the parent through the `search_text LIKE` fallback — root-cause fix in `Indexer::variation_skus()`; a reindex is required for existing products) + `SearchFrontendTest::test_localized_payload_exposes_display_toggles` (the `show` image/price/sku map). Also added four dropdown settings (`max_results` 1–20 clamp in `Ajax`, `show_image`/`show_price`/`show_sku` checkboxes) — schema keys aren't `freeman_*` literals so `baseline-options-declared.txt` is unchanged; Search dropdown toggles saved-value fix (1.21.9) added 1 reported test, +3 reported assertions — `SearchFrontendTest::test_display_toggles_read_saved_integer_values` (the `show` image/price/sku map read `'yes' === get_option(...)`, but `Settings_Hub` persists checkboxes as `1`/`0`, so once the Search settings page was saved every toggle read as off and the dropdown showed names only; `Frontend::localized_payload()` now reads them with `filter_var(..., FILTER_VALIDATE_BOOLEAN)` — the same coercion the Hub uses to render the checked state — handling both the `'yes'`/`'no'` schema default and the saved `1`/`0`). No new strings/hooks/options → baselines + `.pot` unchanged; infinite-scroll shared /page/N/ link fix (1.21.14) net 0 reported tests — JS-only, no PHP touched (Direction A: the default path no longer pushState's `/page/N/` into the address bar as pages auto-load, so a copied URL stays on page 1; Direction B: a fresh non-back/forward landing on `/page/N/` that has a product grid `location.replace`s to page 1 so shared deep-links show the full result set), no JS unit harness in-repo so verified by code review + live DOM-predicate check on arba4, deployed-fix QA pending; QuickView drawer-locale + card-slider arrow fixes (1.21.15) net 0 reported tests — both untested runtime paths (`render_drawer_content()` is live-QA only; card-slider is JS): QuickView wraps the admin-ajax render in `switch_to_locale( get_locale() )` so the buy-box/WC drawer strings render in the site (Hebrew) locale instead of the admin-context English, and card-slider's `scrollToSlide()` scrolls the gallery's own viewport (`scrollBy`) instead of `scrollIntoView` so a card arrow inside a slider-mode ProductSlider no longer scrolls the carousel row (live DOM-simulation verified: track stays put, gallery advances); deployed-fix QA pending; Search storefront-string control + Shop Filters refined panel style (1.21.16) added 7 reported tests, +28 reported assertions — `SearchLabelsTest` (defaults map + saved-override / blank-fallback) + extended `SearchFrontendTest` (payload labels + shortcode placeholder default flow from the new `Search\Labels` settings) + `ShopFiltersModuleTest::test_filter_style_setting_defaults_to_classic`. The Search Labels mirror the QuickView/ShopFilters pattern (per-string `freeman_core_search_label_<key>` options surfaced as text fields). Shop Filters gains a `filter_style` select (`classic` default = current checkbox layout byte-identical; `refined` = pill facets + collapsible chevron headers + numeric show-more + filled chips with circular remove badges + circular drawer-close + contained desktop scroll, all keyed off a `freeman-sf--refined` root class — CSS/JS, live-QA, validated via a static RTL prototype). New option literals `freeman_core_search_label_` + `freeman_core_shop_filters_filter_style` → `baseline-options-declared.txt` regenerated; `.pot` regenerated; Shop Filters refined-style palette + flash fix (1.21.17) added 0 reported tests — CSS/JS only (owner feedback on 1.21.16's refined style): the selected pill was an ink fill + white text, so a theme `:hover{color:ink}` rule turned it ink-on-ink — reworked to the theme ink/paper tokens (`var(--fm-color-*)` + literal fallbacks) with **ink text in every state** (selected = soft fill + inset ink ring + weight), chips to soft pills with a tinted × badge; and made it flash-free (long-list cap moved JS→CSS `:nth-child(n+9)`, chevron moved from a JS-injected span to a CSS `::after`, entry fade + mobile rise cascade disabled under `freeman-sf--refined`), so the panel renders instant on select/reload; ProductSlider grid-image + Shop Filters refined polish (1.21.18) added 0 reported tests — all CSS + a scoped widget filter, live-QA. ProductSlider: `object-fit` flipped `cover`→`contain` on the loop thumbnail so the whole product shows instead of being cropped, and the render path adds a scoped `single_product_archive_thumbnail_size`→`large` filter (removed after the loop) so the grid pulls the larger ≤1024px source instead of the ~324px `woocommerce_thumbnail` that was upscaling blurry on hi-DPI (`woocommerce_single`/600px was tried first but still soft on a full-width archive). Shop Filters (refined style only): the category tree gained its own `max-height`+internal-scroll so a long expanded tree no longer pushes the attribute facets (e.g. Size) below the height-contained desktop panel's scroll fold, and the collapsible-facet chevron was shrunk/softened. No new hooks (`add_filter` is a consumer)/options/strings → baselines + `.pot` unchanged; Search crash resource-cap fix (1.21.19) added 18 reported tests, +24 reported assertions — `SearchRepositorySearchTest` (pure `effective_limit` clamp matrix — the results-page `limit=-1` no longer maps to LIMIT 4294967295 but to the new `MAX_RESULTS=500` ceiling; per-request memo collapses the 3 identical engine reads per search request to one query and `has_data()` to ≤ 1 COUNT, asserted via a counting $wpdb stub; every repository write resets the memo) + new `SearchAjaxTest` (mb-aware `term_meets_min_chars` matrix incl. Hebrew — the dropdown endpoint now enforces `min_chars` server-side). `Results_Query::apply()` early-returns on an empty request term before the `should_handle()` gate, so the index COUNT no longer runs per WP_Query on every non-search page (`should_handle` seam signature unchanged). No new hooks/options/strings → baselines + `.pot` unchanged; Search grid pagination (1.21.20) added 9 reported tests, +10 reported assertions — `SearchResultsQueryTest` `paginate_ids` matrix (rank-ordered page slice, [0] on empty/past-the-end, floors) + `grid_max_pages` passthrough. `constrain_slider_query` now hydrates one page slice (`posts_per_page`, mirroring ShopFilters `products_per_page`) instead of include=all/limit=-1, and the widget's search-page pagination suppression is gone — a NEW additive hook `freeman_core/product_slider/grid_max_pages` lets Search override the page count with the engine total (the widget's `$wp_the_query` can be an unconstrained instance on an Elementor archive — 1.21.5 diagnostic — so its `max_num_pages` is untrustworthy on a search). `baseline-hooks.txt` regenerated: new `grid_max_pages` entry + position-only `query_args` shift; Shop Filters Elementor-grid constraint fix (1.21.21) added 19 reported tests, +22 reported assertions — `ShopFiltersQueryBridgeTest` gained the `is_product_listing_query` matrix (broadened listing gate: WC conditionals OR product post_type OR the query object's own archive predicates incl. registered `pa_*`, with a singular veto), the `should_constrain_slider` matrix (deliberate duplicate of Search's — no cross-module dependency), `compose_include` ×5 (intersect-into-existing preserving the engine's relevance order, `[0]` sentinels), wiring + curated-widget passthrough. Root cause of the live "facet says 2, grid shows all": the old gate was `is_shop()`-only so the Elementor product-archive main query was never constrained, AND Shop Filters had no `freeman_core/product_slider/query_args` listener so the widget's own `wc_get_products()` grid ignored the selection (the Shop Filters twin of Search 1.21.2–1.21.6); new `Query::constrain_slider_query` at priority 20 intersects into Search's include, and a solely-filtered current-query grid gets the current page's slice. No new hooks (`add_filter` is a consumer)/options/strings → baselines + `.pot` unchanged; Shop Filters raw-UTF-8 slug fix (1.21.22) added 1 reported test, +3 reported assertions — `ShopFiltersUrlStateTest::test_encodes_raw_utf8_nonlatin_slug` (PHP urldecodes $_GET, so a percent-encoded Hebrew slug arrives at `parse()` as raw UTF-8; `sanitize_slug()` now percent-encodes non-ASCII before the character filter instead of stripping it — the strip silently dropped the facet from the parsed state, the second half of the "count says 2, click shows all" bug; 1.12.25 only covered still-encoded arrivals). `Query::instock_product_ids` + `Query_Builder::resolve_slug_map` also retry a missed `%`-carrying lookup with the `rawurldecode`d original for importer-created raw-UTF-8 stored slugs. Baselines + `.pot` unchanged; ProductSlider card sizes-attr fix (1.21.23) added 5 reported tests, +5 reported assertions — `ProductSliderCardSizesTest` (`Widget::card_sizes_attr` column-math matrix). The 1.21.18 `large` thumbnail request shipped WP's default viewport-wide `sizes` attr so every card downloaded the biggest srcset candidate (~5-10× the slot's bytes — the main recent-slowdown driver); a render-scoped `wp_calculate_image_sizes` filter now emits the real card slot (≤640px mobile cols incl. slider peek, ≤1024px tablet cols, then a 1400px-content estimate), keeping `large`+DPR sharpness. `baseline-hooks.txt` position-only shift (`grid_max_pages` 1310→1343); Shop Filters facet-response cache (1.21.24) added 3 reported tests, +9 reported assertions — `ShopFiltersQueryBuilderTest` `cache_signature` stability/variance + `ShopFiltersIndexerTest::test_bump_rev_increments_the_index_revision`. `Query_Builder::query()` now serves repeat browse states from a 5-min transient (`freeman_core_sf_q_<md5>`, key = normalised Url_State + context + hide_oos + per_page + index rev + version; search states skipped — engine ids are request-memoized since 1.21.19), ending the whole-catalog postings + double meta_lookup sweep per shop pageview; the ShopFilters `Indexer` bumps the new `freeman_core_shop_filters_index_rev` option (autoload=false) at its three write-completion points for event-driven invalidation. `baseline-options-declared.txt` regenerated (+2 intended additive entries); `baseline-hooks.txt` position-only shift (`search_product_ids` 632→696); RestockNotify CSV formula-injection fix (1.21.25 — remediation PR-2, `docs/remediation-plan-2026-07-03.md`) added 14 reported tests, +16 reported assertions — new pure seam `CSV_Exporter::escape_csv_field` (leading `= + - @ \t \r` gets an `'` prefix per OWASP CSV-injection guidance; dataProvider matrices for dangerous + benign values incl. Hebrew and non-first-char `@`) applied to all data cells in `build_csv`, plus an end-to-end neutralization case; the legacy exporter twin (`legacy/includes/class-rsn-admin.php`) is deliberately untouched pending the B-1 owner decision; CheapestDefaultVariation strategy-select fix (1.21.26 — remediation PR-3) added 2 reported tests, +18 reported assertions — the `strategy` select schema shipped `options` instead of the `choices` key `Settings_Hub::render_field` reads (the 1.16.1 HoverSwap bug's latent twin), so the dropdown rendered empty; one-line key rename + `CheapestVariationHooksTest` schema case + a repo-wide invariant guard iterating all select-bearing module schemas (CheapestDefaultVariation/HoverSwap/InfiniteScroll/ShopFilters) asserting `choices`-not-`options`, making the bug class unrepresentable; ProductFeed silent-failure fix (1.21.27 — remediation PR-5) added 3 reported tests, +14 reported assertions — new pure seam `Generator::promote_feed` returns bool so a failed tmp→final rename no longer records a successful run (no `OPT_LAST_GEN` timestamp, no `after_generate` fire); `write_feed`'s size-sidecar write and `ensure_feed_dir`'s dir-guard write are now result-checked (throw / best-effort warn respectively), and the module's three latent `Logger::info`/`Logger::error` calls — undefined methods that would have fatally re-thrown inside the failure catch — were corrected to `Logger::log(…, <level>)`; `ProductFeedPromoteTest` exercises the seam against real files under the tmp uploads dir (move+true, missing-source→false+error-log, best-effort missing-sidecar→still-true). `baseline-hooks.txt` position-only shift (Generator entries 163→167 / 224→254 / 262→292). **683 tests / 2112 assertions**); ProductSlider meta-orderby off whole-catalog postmeta (1.21.28 — remediation PR-6, audit C1) net +1 reported test, +6 reported assertions — `Widget::fetch_products_by_meta_orderby` no longer primes the whole catalog's postmeta via `update_meta_cache()` per pageview (the biggest per-pageview cost in the suite); the new pure `Widget::sort_values` seam reads the one sort column from `wc_product_meta_lookup` (allowlisted `total_sales`/`average_rating`/`min_price`) in a single indexed `IN()` query, keeping the existing PHP sort + `limit*2` overshoot + visibility tail so tie-break/no-drop semantics are preserved; the sort key for `price` moves from `_price` postmeta to the canonical `min_price` lookup column (owner-approved non-identical output; `rating` moves `_wc_average_rating`→`average_rating`, same values), shipped flagless with git-revert rollback (a flag would keep the slow path alive). `ProductSliderPopularityOrderbyTest` rewritten to drive a `$wpdb` double (SearchRepository pattern) — all 9 sort-order assertions preserved, plus min_price/average_rating column-mapping, single-query, NULL-value→0, and no-lookup-row→0 cases; the live lookup query is integration/live-QA. `baseline-hooks.txt` position-only shift (Widget `grid_max_pages` 1343→1388). **684 tests / 2118 assertions**); ShopFilters dead-native-search short-circuit (1.21.29 — remediation PR-7, audit A/perf) net +1 reported test, +2 reported assertions — `Query_Builder::search_product_ids` now consults a new `null`-default pre-filter `freeman_core/shop_filters/pre_search_product_ids` before building the native product-search `WP_Query`; `Results_Query::pre_supply_engine_ids` supplies the engine-ranked ids there whenever the index has data, so the now-dead native query (its result was already discarded by the post-filter since Search graduated always-on) is skipped on every search-facet build. The existing `search_product_ids` post-filter + `supply_engine_ids` listener are kept for back-compat (Hard Rule #2 + `SearchModuleTest`); the repeated `repo->search` read is request-memoized (1.21.19) so the double-listener is one query. Output byte-identical → additive new filter, no flag. Tests: `SearchModuleTest` asserts the pre-filter registers; `SearchResultsQueryTest::test_pre_supply_engine_ids_respects_prior_listener` covers the pure prior-listener guard (the `has_data()`/engine + `WP_Query`-skip branches are integration/live-QA — `Search_Repository` is final/unstubbable). `baseline-hooks.txt` regenerated: new `pre_search_product_ids` entry + position shift of `search_product_ids` (696→715). **685 tests / 2120 assertions**); ShopFilters facet-cache hardening (1.21.30 — remediation PR-8, audit A2+A3+A4) added 9 reported tests, +14 reported assertions — three economics fixes to the 1.21.24 facet cache: **A2** `cache_signature` gained a `$valid_taxonomies` param and drops junk `filter_<tax>` params (unindexed taxonomies) before keying so probe params can't inflate cache-key cardinality, plus a pure `Query_Builder::should_store_state` that skips `set_transient` when supplied filters resolve to no active term (garbage slugs); **A3** `paged` removed from the key (facet counts are page-invariant) — the cached payload is now the page-invariant `{facets,category_tree,price,flags,count}` and `with_pagination()` rebuilds the pagination+url wrapper per request (response shape unchanged), so `?paged=N` no longer re-sweeps; **A4** the drain path bumps via a new debounced `Indexer::maybe_bump_rev` (≥`REV_DEBOUNCE`=60s between bumps, tracked by the new `freeman_core_shop_filters_index_rev_at` option) so steady orders don't retire the whole cache per sale — reconcile/full-rebuild keep the immediate `bump_rev` (its public always-bump contract + test preserved) — plus a best-effort single-flight rebuild lock (`acquire/release_rebuild_lock`, 30s) so concurrent misses don't stampede the same option row. The indexed-taxonomy set is memoised per request so the cache-hit path pays ≤1 DISTINCT. Accepted trade-offs: facet counts can lag a stock change by ≤60s (5-min TTL backstops); the lock is write-dedup only (no serve-stale — deferred). Tests: `ShopFiltersQueryBuilderTest` (page-invariance, unindexed-tax-drop, indexed-variance-kept, `should_store_state` dataProvider ×4, single-flight lock) + `ShopFiltersIndexerTest::test_maybe_bump_rev_debounces_within_window`; the live cache read/write is integration/live-QA. `baseline-options-declared.txt` +1 (`…_index_rev_at`); `baseline-hooks.txt` position-only shift (ShopFilters search hooks 686→802 / 715→831). No flag (transparent-cache hardening; revert → per-request build). **694 tests / 2134 assertions**); ShopFilters facet-build efficiency (1.21.31 — remediation PR-9, audit A5+A6+A7) added 4 reported tests, +11 reported assertions — three build-path micro-optimisations, all output byte-identical (no flag; revert-only rollback): **A5** `Query_Builder::prime_terms` warms WP's term + term-meta object caches for the whole taxonomy in one `get_terms(include=…, update_term_meta_cache=true)` before `build_term_index`/`build_category_meta`, so their per-term `get_term()`/`get_term_meta()` reads hit cache instead of firing 2-4 queries each (the win fully materialises with a persistent object cache; live-QA/query-count spot check); **A6** merged `product_prices()`+`product_flags()` — two identical-id `wc_product_meta_lookup` sweeps over the same term-filtered set — into one `product_meta()` SELECT (`min_price,max_price,onsale,stock_status`) + the pure `Query_Builder::shape_product_meta` seam that splits rows into the parallel price/flag maps; **A7** `Query::instock_product_ids` is now memoised per request (keyed by the serialized filter selection) so the same selection resolved up to 3× per request (main-query post__in + slider constraint + search enforcement) pays one index read, and `Query::index_has_data` reads the new `Index_Repository::has_rows()` (`SELECT 1 … LIMIT 1` existence probe) instead of `count_rows()`'s `COUNT(*)` (`count_rows()` kept — the admin status surface needs the real count). Tests: `ShopFiltersQueryBuilderTest` (`shape_product_meta` row→prices/flags mapping, empty rows, missing-column defaults) + `ShopFiltersQueryBridgeTest::test_instock_resolution_is_memoized_per_request` (counting-`$wpdb` stub — `constrain_slider_query` twice ⇒ one `get_col`, has_rows ≤1 `get_var` — the SearchRepositorySearchTest pattern); the live term-prime + merged read are integration/live-QA. Added `get_term_by`/`is_wp_error` bootstrap stubs (test infra). `baseline-hooks.txt` position-only shift (ShopFilters search hooks 802→808 / 831→837). **698 tests / 2145 assertions**); indexer background-churn reduction (1.21.32 — remediation PR-10, audit B1+B3) added 8 reported tests, +8 reported assertions — a mirrored change across the twin indexers (Search + ShopFilters, 2 modules stated per Hard Rule #4), output/index byte-identical, no flag: **B1** `Indexer::ensure_scheduled()` now early-returns unless the new pure `Indexer::should_ensure_scheduled(is_admin, is_cron)` passes, so the Action Scheduler existence SELECT (`as_next_scheduled_action`) no longer runs on every storefront pageview — only admin/cron requests (the init hook stays wired; activation is admin + the wp-cron loopback is cron, so the schedule self-heals). **B3** both `run_reconcile()`s drop the idle/caught-up-tick `WATERMARK` re-park (the `update_option` on the empty-`WP_Query` branch) — a lagging watermark is correct (the `post_modified_gmt > watermark` window only ever contains genuinely-new modifications; an empty window is a cheap indexed seek) and internal-only; the `LAST_RUN` heartbeat write is kept (it backs the admin "Last run" status display). **B4** (mark_dirty shutdown-batching) deferred — the reconcile sweep already backstops the lost-update risk. Tests: `ShopFiltersIndexerTest`/`SearchIndexerTest` `should_ensure_scheduled` gate matrix (dataProvider ×4 each); `run_reconcile`'s empty-branch skip is integration/live-QA (needs WooCommerce + WP_Query). No hooks/options/strings → baselines + `.pot` unchanged. **706 tests / 2153 assertions**); ShopFilters grid per-page source of truth (1.21.33 — remediation PR-12, audit) added 5 reported tests, +5 reported assertions — the `constrain_slider_query` page slice (`Query.php`, consequential) and the advisory `products_per_page` count (`Query_Builder.php`, cosmetic) read the blog `posts_per_page` instead of the WooCommerce shop grid size, so a store whose shop grid ≠ blog per-page mispaginated the filtered grid. Fix: both resolve via the new `Query_Builder::resolve_per_page($wc, $blog)` (pure — WC value if `>0`, else blog if `>0`, else 12, mirroring the old `> 0 ? : 12` clamp incl. the -1/"show all" edge) fed by `Query_Builder::wc_grid_per_page()` (integration — `apply_filters('loop_shop_per_page', wc_get_default_products_per_page())`, 0 when WC absent → fallback). Byte-identical where blog == shop per-page (the arba4 case); no flag (bugfix, revert rollback). Tests: `ShopFiltersQueryBuilderTest::test_resolve_per_page_prefers_shop_grid_then_blog` (dataProvider ×5); the live `loop_shop_per_page` read is integration/live-QA. `baseline-hooks.txt` +1 intended additive entry (the WC `loop_shop_per_page` consumer — not a Freeman hook declaration); no options/strings → `.pot` unchanged. **711 tests / 2158 assertions**); ShopFilters indexer lighter variation reads (1.21.34 — remediation PR-11, audit B2) added 0 reported tests — `Indexer::reindex_product` no longer builds the full frontend payload per variation (array-mode `get_available_variations()`: image srcset + `price_html` + `availability_html`/`wc_get_stock_html`, ~10+ queries/variation) just to read stock flags; the new private `variation_stock_payload()` maps `get_available_variations('objects')` — WooCommerce's identical availability filtering (exists / hide-out-of-stock / hide-invisible + `variation_is_visible`), same variation set, only the heavy per-variation assembly skipped — to the minimal `{is_in_stock, is_purchasable, attributes}` shape `Term_Helpers::in_stock_values_by_attribute()` consumes. Parity is by construction: the three fields are sourced exactly as WC's array payload sources them (`is_in_stock()` / `is_purchasable()` / `get_variation_attributes()`), so the stock map is byte-identical; the one divergence (array mode runs the `woocommerce_available_variation` payload filter, `objects` mode doesn't) is ruled out by the staging full-rebuild index parity diff. Old-WC guard: a non-`WC_Product_Variation` element (pre-3.4 ignored the `'objects'` arg) falls back to the array payload as-is. No flag (internal optimization, parity-preserved output, git-revert rollback — a flag would keep the slow path alive). No new unit tests — `variation_stock_payload` is object-dependent/integration and its pure consumer is already covered; `ShopFiltersIndexerTest` stays green as the wiring guard. No hooks/options/strings → baselines + `.pot` unchanged. Verification: parity-by-construction + staging full-rebuild `freeman_shop_filter_index` byte-diff + the 1.12.14 sold-out-size-drops-out live-QA on arba4. **711 tests / 2158 assertions**); VariationSwatches swatch-tooltip strings through Labels (1.21.35 — remediation PR-13) added 3 reported tests, +6 reported assertions — `VariationSwatchesI18nPayloadTest` (the new pure `Etucart_VS_Frontend::i18n_payload()` seam exposes `oos`/`unavailable` keys resolving He/En via `Labels::get`). The swatch-tooltip out-of-stock/unavailable wording in `etucart-swatches.js:219` previously bypassed the locale-aware `EtucartVS.i18n` payload as hard-coded Hebrew literals (English-locale sites saw Hebrew tooltips); the JS now reads `i18n.oos`/`i18n.unavailable` with the Hebrew literals retained only as the missing-global fallback, and the legacy localize gains an additive `unavailable` key (owner-approved Hard Rule #3 legacy edit — one additive array line adjacent to the existing `oos` line, extracted into the testable `i18n_payload()` static). No new hooks/options/strings; the `etucart_vs_checkout_url` hook didn't shift → baselines + `.pot` unchanged. The JS read is live-QA. **714 tests / 2164 assertions**); RestockNotify locale correctness (1.21.36 — remediation PR-14) added 2 reported tests, +2 reported assertions — `RestockNotifyEmailTest::test_html_dir_attrs_*` (the new pure `Email::html_dir_attrs($locale, $is_rtl)` seam → `he_IL`/rtl ⇒ `he-IL`/`rtl`/`right`; `en_US`/ltr ⇒ `en-US`/`ltr`/`left`). The restock email shell hard-coded `lang="he" dir="rtl"` + `direction:rtl;text-align:right`, so an English-locale site rendered English copy inside an RTL Hebrew shell; `build_html()` now derives the `<html lang>`/`dir` + inline direction/text-align from `get_locale()` + `is_rtl()`, and `frontend.js`'s `rsn_ajax`-undefined script-config error routes through the `t()` locale helper (Hebrew literal kept as the fallback; runtime byte-identical since `rsn_ajax` is undefined in that branch). Test infra: `is_rtl` promoted from a null stub to a locale-aware smart stub (reads `$GLOBALS['fr_locale']`, RTL for he/ar/fa/ur/yi/dv/ps/sd) so the RestockNotify email snapshot goldens render the correct per-locale direction (regenerated: He ⇒ `he-IL`/`rtl`, En ⇒ `en-US`/`ltr` — shell-attr lines only). Legacy RSN email twin untouched (B-1). No new hooks/options/strings; `.pot` unchanged; `baseline-hooks.txt` position-only shift (Email.php 209→231 / 292→314). The JS `t()` route is live-QA. **716 tests / 2166 assertions**); VariationSwatches font-token routing (1.21.37 — remediation PR-15) added 0 reported tests — CSS-only: the 5 `font-family` sites in `etucart-shop-swatches.css` (×2) + `etucart-swatches.css` (×3) that referenced the Elementor Style Kits variable directly (`var(--e-global-typography-sk_type_12-font-family, inherit)`) now route through the theme design token (`var(--fm-font-body, inherit)`), which the theme already maps to `sk_type_12` with a Heebo/Rubik fallback ([freeman-tokens.css](../freeman-theme/assets/css/freeman-tokens.css); same idiom as `my-account.css`). The `inherit` fallback is preserved, so non-theme installs and theme sites with `sk_type_12` configured (arba4) are byte-identical; only a theme site with `sk_type_12` *unset* changes (inherit → the theme's body stack, the intended token routing). Scope note: the plan predicted a `freeman-theme` half + theme bump, but the token mapping already existed and was complete — owner-approved reduction to core-only, no theme change/bump, no `--fm-font-heading` alias added (nothing consumes heading typography in plugin CSS). No flag (CSS-only, revert rollback); no hooks/options/strings/tests → baselines + `.pot` unchanged. Live-QA: swatch typography unchanged on arba4. **716 tests / 2166 assertions**); Z-index token adoption (1.21.38 — remediation PR-16) added 0 reported tests — CSS-only: the high z-indexes in VariationSwatches (`etucart-swatches.css` sticky-bar `9999!important`→`var(--fm-z-modal,9999)`, toast `10000`→`var(--fm-z-toast,10000)`; `etucart-shop-swatches.css` toast-stack `10000`→`var(--fm-z-toast,10000)`), ShopFilters (`shop-filters.css` overlay `99998` + panel `99999`→`var(--fm-z-max,…)`) and QuickView (`quick-view.css` drawer `100000`→`var(--fm-z-max,100000)`) now route through the theme `--fm-z-*` ladder ([freeman-tokens.css](../freeman-theme/assets/css/freeman-tokens.css):169-174), with each current literal kept as its own fallback so non-theme sites are byte-identical. The token set tops out at `--fm-z-max`(99999), so on a theme site QuickView + both ShopFilters drawer layers collapse to 99999; the ordering (QuickView over ShopFilters over sticky header) is preserved by DOM order — QuickView renders at `wp_footer` (later in the DOM) while the ShopFilters panel renders inline via its shortcode template, so the later element wins the tie (documented inline on the QuickView rule; owner-approved over adding a new theme token). No flag (CSS-only, backward-compatible fallbacks, revert rollback); no hooks/options/strings/tests → baselines + `.pot` unchanged. Deferred live-QA: QuickView drawer over ShopFilters drawer over sticky header on arba4 (mobile + desktop). **716 tests / 2166 assertions**). The standing reporting unit going forward is `<reported tests> / <assertions>`. When updating this number, run `vendor/bin/phpunit` and copy the reported total verbatim. Runs in CI on PHP 8.0–8.3 lanes (PHP 7.4 dropped 2026-05-03 / 1.11.22 — shipped freeman-core already used PHP 8.0+ idioms baked in by Wave 2.3a–c, so the 7.4 PHPUnit lane was de-facto failing; aligning CI to reality rather than retrofitting polyfills, since no client site runs PHP 7.4). Locally: `composer install` once, then `PATH="/opt/homebrew/opt/php@8.3/bin:$PATH" composer test` (system `php` is 8.5, outside PHPUnit 10's supported range). freeman-digital has its own separate test config; freeman-theme has none.
- **Snapshot harness**: shipped in Wave 0.5. See `/tests/snapshots/` (`SnapshotTestCase` trait, `Scrubber`, three example tests with committed goldens) and `/tests/snapshots/README.md`.
- **Staging**: provisioned (owner-confirmed 2026-07-05). Access details (WP-CLI alias / SSH) TBD — capture them here when first used. Used for e.g. the PR-11 full-rebuild index parity diff.
- **WP-CLI**: installed locally (v2.12.0; verified 2026-05-28). Used for `.pot` regeneration (`wp i18n make-pot <pkg> <pkg>/languages/<pkg>.pot --domain=<pkg>`).
- **Commit format**: Conventional Commits, scope = package. Example: `feat(freeman-core): add logger hooks`.

If any of the above changes, update this section before starting work.

## Hard rules — NEVER violate

These extend §3 (Surgical Changes) with project-specific surfaces:

1. NEVER ship a roadmap item without a feature flag (`freeman_core_<module>_<feature>_enabled`, default `false`) unless purely additive (new hook, new filter, new CSS variable with backward-compatible fallback, or a new helper class with no callers).
2. NEVER remove an existing hook, filter, option key, shortcode, REST route, or admin URL. Deprecate via `_deprecated_function()` / `_deprecated_hook()` with a 2-minor-version sunset.
3. NEVER edit `legacy/` directories or `etucart_*` keys without a written migration plan AND human approval.
4. NEVER touch more than ONE roadmap item per PR. Exception: the 18-hooks PR (Wave 1.1) — state it explicitly.
5. NEVER bump a major version without explicit instruction.
6. NEVER modify the database schema without `dbDelta()` upgrade routine + downgrade note.
7. NEVER change `Logger`'s `final` keyword. Do not extract a `Logger_Interface`. Add hooks only inside `log()`.
8. NEVER use `error_log()`, `var_dump()`, or `console.log()` in shipped code. Use `Freeman\Core\Core\Logger`.
9. NEVER ship a wave PR without updating `/docs/roadmap.md` in the same PR — mark the wave's section with `✅ shipped <version> (#<PR>, <YYYY-MM-DD>)` and bump the **Last updated** line. Sub-PRs (e.g. 1.1a, 2.3a) update only their own sub-bullet; the parent wave stays open until all sub-PRs ship. If the same PR also lands roadmap edits beyond the shipped-marker (promotion, scope change, new wave entry), state that separately in the PR description.

## STOP and ask before coding if

This extends §1 (Think Before Coding) with concrete project triggers:

- The roadmap item depends on something not resolved in `/docs/decisions-2026-04-28.md`.
- The plan requires touching `legacy/` or `etucart_*` keys.
- The plan requires a database schema change.
- The plan requires a major version bump.
- Backward-compat cannot be honored without trade-offs.
- A single change request implies more than one roadmap item.

## Per-PR contract

Every PR must include the sections from `/docs/pr-template.md`. Reviewers reject PRs missing the Backward Compatibility section, the Feature Flag declaration, or the Rollback Plan.

If touching >12 files or >3 modules: STOP and ask whether to split.

## Output format for roadmap work

When asked to implement a roadmap item, follow §4 (Goal-Driven Execution) with this concrete shape:

1. **Pre-flight**: which decisions in `/docs/decisions-2026-04-28.md` does this depend on; which Wave-0/1 prerequisites are satisfied. Include a **Roadmap delta** section listing roadmap changes, or `none` if truly none, as a flag to double-check. Roadmap freshness: every wave's PR must update `/docs/roadmap.md` in the same PR by marking the wave shipped with version + date, bumping the "Last updated" line, and reconciling any scope drift between predicted and actual. **If the wave adds tests**, note that the "Current infrastructure state" PHPUnit count above must be updated in the same PR — copy the new totals (reported tests / assertions) from `vendor/bin/phpunit` verbatim.
2. **Plan**: file list, hook list, option keys, feature flag name, default value, test list. **No code yet.** When sealing the file list, anticipate these mechanical files up front so they don't surface mid-implementation:
   - **Version bump touches two PHP files**, not one. `tools/release.sh`'s `bump_core` always edits both `freeman-core/freeman-core.php` (header + `FREEMAN_CORE_VERSION` constant) AND `freeman-core/src/Core/Plugin.php` (`const VERSION`). Any wave bumping `freeman-core` must list both.
   - **Hook-bearing file mods imply baseline regeneration.** `tests/BaselinesIntegrityTest` re-runs `tools/capture-baselines.sh` and asserts byte-identity against committed baselines. Modifying any file containing the corresponding surface drifts line numbers and breaks the assertion — mechanical, no behavioral change. Anticipate the matching baseline file in §5 ahead of implementation:
     - file contains `apply_filters` or `do_action` → `tests/baseline-hooks.txt`
     - file contains `register_rest_route` → `tests/baseline-rest.txt`
     - file contains `WP_CLI::add_command` → `tests/baseline-cli.txt`
     - file declares a `freeman_` or `etucart_` option key → `tests/baseline-options-declared.txt` and/or `tests/baseline-options-legacy.txt`
     Regenerate via `bash tools/capture-baselines.sh` and commit verbatim; never hand-edit. The committed diff must consist only of line-number / position changes on existing entries — anything else means a real surface change snuck in and the §6 inventory is stale.
   - **Version bumps and test-count changes touch CLAUDE.md.** The "Current infrastructure state" section names the current `freeman-core` version (Repo state line) and `<reported tests> / <assertions>` (PHPUnit line). Any wave that bumps the version or adds tests moves these values; CLAUDE.md must appear in §5 alongside the version-bump file pair so the staleness doesn't get treated as a follow-up chore. Pre-flight §1 already calls this out for tests; §5 must echo it so the file actually gets sealed in. Bump the *Last verified* date in the same edit — the date is the truthfulness stamp on the values, not adjacent content.
3. *Wait for human approval of the plan before proceeding.*
4. **Execution**: code changes, one logical commit per file group.
5. **Verification**: snapshot diff, test results, manual QA checklist filled in.
6. **Rollback**: exact `wp option update` command to disable.

Skipping step 1 or 2 violates §1.

## First task on a fresh session

1. Confirm you've read this file, `/docs/decisions-2026-04-28.md`, and `/docs/roadmap.md`.
2. Verify the "Current infrastructure state" section above matches reality. Flag drift.
3. Ask which roadmap item to start on.
4. Once given an item, follow the Output format — pre-flight + plan first, no code.
