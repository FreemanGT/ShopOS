# ShopOS Plugin Suite — Roadmap

**Last updated**: 2026-07-17 (forward-pointer to post-Wave-9 ledgers added — see the ⚠️ box below; this wave log itself is frozen at Wave 9) — prior: 2026-07-06 (Wave 9 — Product Page new module — 9.3 ✅ shipped 1.24.0 + gallery follow-ups 1.24.1–1.24.3 (#114): PDP live-QA round 2 + perf pass — gallery image-size root-cause fix (`woocommerce_gallery_image_size` → `woocommerce_single`; slider-support removal had dropped non-main images to ~100px thumbs) + gallery layout (1.24.3): one swipeable image at a time at every breakpoint (scroll-snap carousel + progress bar floored at ~14%), desktop mouse click-drag-to-scroll, no lightbox (capture-phase handler + expand trigger hidden), related-grid 3+1/mobile fix (WC loop-CSS specificity + clearfix pseudo grid-items + args pinned 4/4), Additional information now a collapsed `<details>` with kit-proof typography, coupon notice moved under the buy box (prio 31) as a visible white card, buy-box full-width `!important` pin (kills the ~50%-left layout + right→left paint flash from a site-level legacy rule), title `text-wrap:balance` dropped, and the PDP perf pass — shared per-request `Variations` memo collapsing the coupon+urgency double `get_available_variations()` sweep (QuickView site-wide assets + CheapestDefaultVariation `pdp_only` check = follow-up). Prior: 9.2 ✅ shipped 1.23.1 (#112): PDP live-QA follow-up — gallery flexslider-blank fix + lightbox→in-image zoom, VS buy-box reverted to its native red + owner "button colour" hex setting, coupon-price now inline (was wrapping below the text), trust line + Additional-information surfaced directly under the buy box, subtle summary panel + 1rem image/panel radii, VS price forced to ink, mobile gallery progress bar replacing the dot rail + sticky-bar space reservation; the express-checkout/Google-Pay item was an owner-side Stripe setting, not our code. Prior in Wave 9: flag-graduation sweep ✅ 1.23.0 (ProductPage + 6 other modules graduated to always-on, flags hard-removed); 9.1a ✅ 1.22.1 design critique follow-up; 9.1 ✅ shipped 1.22.0: designed PDP template takeover + coupon-price notice + low-stock urgency badge — see Wave 9 below. Prior: Wave 8 — Search new module — Phase 8.1 foundation ✅ shipped 1.18.0 (`shopos_search_index` FULLTEXT table + dirty-queue indexer + "Reindex all" tool behind `shopos_core_search_indexer_enabled`), Phase 8.2 live AJAX dropdown ✅ shipped 1.19.0 (ranked MATCH...AGAINST read query + public endpoint + debounced combobox dropdown behind `shopos_core_search_dropdown_enabled`) + 8.2a `[shopos_search]` shortcode ✅ 1.19.1, Phase 8.3 results page ✅ shipped 1.20.0 + 8.3a live-QA fixes ✅ 1.20.1 (blank-grid LIMIT -1 fix + selector-setting removal), Phase 8.4 graduation ✅ shipped 1.21.0 (module graduated to always-on, three `search` flags hard-removed — the planned parity-tooling/cutover was obsoleted by the owner deactivating AWS mid-wave); **Wave 8 epic complete** — in-house product search now replaces Advanced Woo Search end-to-end. Prior: Wave 7 — Quick View + grid parity — opened per decisions §6: Phase 7.0 roadmap + decision record landed, Phase 7.1 parity audit ✅ docs + 7.1a core fix ✅ 1.12.31 + 7.1b theme fix ✅ 1.11.26, Phase 7.2 QuickView module ✅ shipped 1.13.0 — per-card trigger + inline-end drawer + AJAX endpoint behind `shopos_core_quick_view_frontend_enabled`, Phase 7.3 QA follow-ups ✅ shipped 1.14.0 — ProductSlider current-query grid acts as archive grid + pagination, Quick View drawer full-width + icon restyle; **Wave 7 epic complete**. Prior: Wave 6 — Shop Filters new module — Phase 6.1 foundation ✅ shipped 1.12.0, Phase 6.2 facet engine ✅ shipped 1.12.1, + 1.12.2 indexer bug-fix + 1.12.3 admin toggle/status surface + 1.12.4 sweep-scheduling fix + 1.12.5 status/sweep QA polish, Phase 6.3a frontend read path ✅ shipped 1.12.6 + 1.12.7 (6.3a.1) read-path fix — real query bridge + reload transport + 1.12.8 (6.3a.2) stock-visibility parity + 1.12.9 (6.3a.3) index diagnostic + 1.12.10 (6.3a.4) hyphen-taxonomy + pagination fix, Phase 6.3b Facet UI ✅ shipped 1.12.11 — category-tree facet (navigation) + colour/image swatches + panel styling + mobile drawer + render hooks, Phase 6.3c storefront string labels ✅ shipped 1.12.12 — editable panel wording (blank = English default), enabling Hebrew without code, Phase 6.5b price facet + sort ✅ shipped 1.12.13 (built ahead of 6.4/6.5a — price bands on top + sort dropdown + site default-sort; on-sale/in-stock deferred to 6.5c) + 1.12.14 in-stock-filtering bug-fix (grid + counts now use the index's per-variation in-stock truth, so a sold-out size drops out) + 1.12.15 UI polish (minimal/rounded panel buttons via theme `--shopos-ui-*` tokens) + 1.12.16 button restyle follow-up (panel buttons now use the theme's `.shopos-ui-btn` / `.shopos-ui-select` primitives so they match the site's buttons; 1.12.15 core styles were theme-overridden) + 1.12.17 search-results faceting (panel facets/counts/tree/price now scope to the search-result set; resolves the 6.3a.1 deferral) + 1.12.18 AWS search-grid enforcement attempt (`the_posts` safety net — did NOT resolve it; AWS isn't rendering from the main query loop, see OPEN note below) + 1.12.19 desktop-toggle fix (mobile Filter button no longer shows on desktop) + 1.12.20 AWS search enforcement at priority 99999 (run last — QA-confirmed; search now filters correctly end-to-end), Phase 6.4 admin facet config ✅ shipped 1.12.21 — per-attribute matrix (show / order / hide-on-categories) writing the facet_config option, flag `shopos_core_shop_filters_admin_config_enabled` (off = auto-derived defaults); per-facet type selector dropped as a no-op on this storefront, Phase 6.5a filtered-URL SEO policy ✅ shipped 1.12.22 — noindex,follow + clean-archive canonical on filtered URLs, routed through RankMath / SEOPress / Yoast / core, flag `shopos_core_shop_filters_seo_policy_enabled` (default off), Phase 6.5c on-sale/in-stock facets ✅ shipped 1.12.23 — On sale (+ In stock when the store shows OOS) panel checkboxes from wc_product_meta_lookup, reuses frontend flag; **Wave 6 epic complete**. Phase 6.6 — module graduated to always-on, all four `shop_filters` flags hard-removed + Index diagnostic removed — ✅ shipped 1.12.26. Prior: Wave 5.1 — Feature Flags admin page — ✅ shipped 1.11.44; Wave 2.2 / 4g — VariationSwatches settings surface graduated, `settings_hub` flag retired — ✅ shipped 1.11.45)
**Owner**: Yiftach
**Reflects decisions in**: `/docs/decisions-2026-04-28.md`

This is the original wave-based execution plan (Waves 0–9). Waves run in order. Items within a wave can run in parallel only if they touch separate modules.

> **⚠️ Post-Wave-9 work does NOT live in this file.** This log stops at Wave 9 (2026-07-06 / core 1.24.x). Everything shipped since is tracked in [expansion-roadmap-2026-07.md](expansion-roadmap-2026-07.md) (Phases 0–4) and [decisions-2026-04-28.md](decisions-2026-04-28.md) §11 (the ShopOS Line, §11.4 row status), with per-release detail in [CLAUDE.md](CLAUDE.md)'s "Current infrastructure state". A fresh session must read those before assuming Wave 9 is the frontier. In-flight/shipped since this file was last a live log:
> - **Wave 9.3 follow-up train** (core 1.24.4–1.24.12): mobile search palette, SKU-infix ranking fixes, InfiniteScroll history-default flip + back-nav restore hardening, filtered-search blank-grid fix, storefront FOUC/skeleton fixes.
> - **PageTransitions** — a NEW module (core 1.25.0/1.25.1; default OFF): scrim+spinner overlay + cross-document View Transitions for the reload-transport UX.
> - **Dashboard self-updates / release channel** (core 1.42.0/1.42.1 + theme 1.12.0/1.12.1): manifest-driven updater with a 5-min cache.
> - **Expansion Phases 0–3** (core 1.26.0–1.41.0): Widget_Base extraction, ShopOS Elementor category, Settings field API, Labels_Base, per-module Elementor widgets, Cache facade, Design panel (§9), scoped `wp shopos` CLI, Perf-budget tooling, Store Blueprint (§10), dashboard search.
> - **The ShopOS Line / §11.4** (theme 1.11.29–1.14.0 + core 1.42.2–1.44.0): CSS-chain fix (row 2), self-hosted webfonts + `fonts_selfhost` flag (row 3), theme-owned PDP (row 4) and PLP + shared theme loader (row 5). Both template flags default OFF, flip gated on §11-B staging preconditions.
> - **Remediation follow-through** (core 1.44.2/1.44.3, digital 1.7.8): B-1 legacy-CSV escape, B-3 InfiniteScroll stub withdrawal, PR-18 admin escaping — the 2026-07-17 full-sweep close-out.

## Shipped to date

| Wave | Description | Version | Merged | PR |
|---|---|---|---|---|
| 1.1a | Module hooks (gates / data filters) — 3 hooks | shopos-core 1.11.0 | 2026-04-28 | [#6](https://github.com/FreemanGT/ShopOS-theme/pull/6) |
| 1.1b | Module hooks (render and feed) — 7 hooks + 2 snapshots | shopos-core 1.11.1 | 2026-04-28 | [#7](https://github.com/FreemanGT/ShopOS-theme/pull/7) |
| 1.2 | RestockNotify locale bootstrapper (English defaults, Hebrew opt-in) | shopos-core 1.11.2 | 2026-04-28 | [#9](https://github.com/FreemanGT/ShopOS-theme/pull/9) |
| 2.3a | RestockNotify modern Subscribers repository wrapper | shopos-core 1.11.3 | 2026-04-29 | [#10](https://github.com/FreemanGT/ShopOS-theme/pull/10) |
| 2.3b | RestockNotify modern Email + Stock_Monitor (bilingual fix + 2 hooks) | shopos-core 1.11.4 | 2026-04-29 | [#11](https://github.com/FreemanGT/ShopOS-theme/pull/11) |
| 2.3c | RestockNotify modern Frontend (Hebrew-JS-strings fix + `should_inject` hook) | shopos-core 1.11.5 | 2026-04-29 | _this PR_ |

Wave-1.1's `infinite_scroll/selector` / `before_render` / `after_render` are still folded into Wave 3.1. With 2.3c shipped, all 3 deferred Wave-1.1 RestockNotify hooks are now live (`should_inject` from this wave; `email_args` + `before_send` from 2.3b). Wave 2.3 closed; Wave 3 (P1 functional improvements) is next.

---

## Active items (2)

The original 15-item roadmap was reduced to 9 after the strategic decisions (dropped: #7, #8, #13, #14, and parts of #15; see `/docs/decisions-2026-04-28.md`). Of the remaining 9, seven shipped through Waves 1.1, 1.2, 2.2, 3.1, 3.2, 3.3, 4.1, 4.3 — see the "Shipped to date" table and wave sections below. #11 partially shipped via Wave 4.2 (CategorySlider variant only); ProductSlider variant remains open.

| # | Priority | Module | Description |
|---|---|---|---|
| 3 | P0 | ProductFeed | Multi-channel feed support (only if a client needs it; otherwise P2) |
| 11 | P2 | ProductSlider | Expose hardcoded design tokens as Elementor controls (CategorySlider variant shipped Wave 4.2 / 1.11.39) |

**Net-new epic (2026-05-20):** Wave 6 — **Shop Filters** (new module, faceted AJAX product filters) — replaces the third-party Filter Everything Pro. Built flag-gated per phase; graduated to always-on (all flags hard-removed) in 1.12.26 (Phase 6.6). Not an original-audit item. See Wave 6 below.

---

## Wave 0 — Safety net (must land before any feature work)

These items are prerequisites. Nothing in Wave 1+ can start until Wave 0 is complete.

**0.0 — Set up PHPUnit**
- Configure `/tests/phpunit.xml` and `/tests/bootstrap.php`
- Add one example test that passes
- Document how to run tests in `/tests/README.md`

**0.1 — Add Logger extension hooks (D8)**
- Add `shopos_core/logger/entry` filter inside `Logger::log()`
- Add `shopos_core/logger/written` action after the option write
- Logger stays `final`. Do not extract an interface.
- Test: filter mutates entry, action fires with correct payload

**0.2 — Feature-flag helper**
- Add `ShopOS\Core\Core\Feature_Flags::is_enabled($module, $feature)`
- Reads `shopos_core_<module>_<feature>_enabled`
- Default `false`
- Document the convention in `/docs/feature-flags.md`

**0.3 — Settings export/import tool**
- New admin page under ShopOS menu: "Tools"
- Export: dump all `shopos_core_*` and `shopos_digital_*` options as JSON
- Import: validate JSON shape, write options, log changes
- This is the rollback path if a later change corrupts settings

**0.4 — Regression baseline**
- Run and commit:
  - `wp option list | grep shopos_` → `/tests/baseline-options.txt`
  - `grep -rn "apply_filters\|do_action" shopos-core/src/` → `/tests/baseline-hooks.txt`
  - `grep -rn "register_rest_route" shopos-*` → `/tests/baseline-rest.txt`
  - `grep -rn "WP_CLI::add_command" shopos-*` → `/tests/baseline-cli.txt`
- These are the snapshots Wave 1+ must not break

**0.5 — Snapshot harness**
- Create `/tests/snapshots/` with helpers to capture:
  - Rendered HTML for any module's frontend output
  - Generated XML for ProductFeed
  - JSON dump of any settings page
- One example per type, committed
- This unlocks the "diff must be empty when flag is OFF" requirement

---

## Wave 1 — P0 extensibility (additive, zero behavior change)

**1.1 — Add module hooks (Roadmap #1)** ✅ shipped — see "Shipped to date" table above

Originally scoped as 18 hooks across 9 modules per `/docs/archive/audit-2026-04-28.md` §D1. After per-call-site reality check during pre-flight (2026-04-29), only 10 of those hooks have viable call sites in non-legacy `shopos-core` PHP today (one further hook — `infinite_scroll/selector` — was dropped from 1.1a after finding the JS doesn't yet read it; folded into Wave 3.1 instead). Wave 1.1 ships the implementable subset; the rest are deferred to natural homes (see Wave 2.3, Wave 3.1, Wave 3.4 below; VariationSwatches's 2 hooks already deferred to Wave 2.2).

Split into two PRs (waiver from "one roadmap item per PR" — stated in each PR description):

**1.1a** ✅ shipped 1.11.0 (#6, 2026-04-28) — gates / data filters (3 hooks, no rendering refactors). Lands shared infra: 1.11.0 version bump, CLAUDE.md infra-state refresh, this roadmap edit.
- `shopos_core/cheapest_variation/should_apply` (filter)
- `shopos_core/cheapest_variation/chosen` (filter — replaces audit's `strategy` because the picker returns an attributes array, not a variation_id)
- `shopos_core/variable_stock_fix/should_check` (filter)

**1.1b** ✅ shipped 1.11.1 (#7, 2026-04-28; forward-merged via #8) — render and feed (7 hooks; 2 snapshot tests).
- `shopos_core/category_slider/query_args` (filter)
- `shopos_core/category_slider/render_card` (filter, via output buffering)
- `shopos_core/product_slider/query_args` (filter)
- `shopos_core/product_feed/query_args` (filter)
- `shopos_core/product_feed/item` (filter)
- `shopos_core/product_feed/before_serve` (action)
- `shopos_core/product_feed/after_generate` (action)
- New CategorySlider snapshot + ProductFeed byte-identity snapshot

Standards for both:
- Each hook: `@since 1.11.0` PHPDoc with `@param` descriptions
- Each hook: at least one test verifying the filter mutates output / the action fires
- With no listeners attached, output must be byte-identical to current

### Deferred from 1.1

Wave 2.3 and the Wave 3.1 expansion below are committed work, approved 2026-04-29 alongside Wave 1.1a. Wave 3.4 remains proposed (not yet approved); promote only once an internal MyAccount extension need surfaces.

**1.2 — RestockNotify locale bootstrapper (Roadmap #2)** ✅ shipped 1.11.2 (#9, 2026-04-28)
- Per `/docs/decisions-2026-04-28.md` §4.2: English defaults, Hebrew opt-in
- Create `RestockNotify/locales/en_US.php` and `he_IL.php`
- On activation: detect `get_locale()`, install matching defaults
- **Existing installs**: do NOT overwrite their option values (check if values exist first)
- Email-body **option** strings ship per locale; the email **shell** strings (greeting / unsubscribe link / suffix / customer-name fallback) stayed hardcoded Hebrew in `legacy/class-shopos-restock-email.php` until Wave 2.3b's full bilingual fix
- Reading A from pre-flight (no `legacy/` edits) — locale files = data; send path unchanged in 1.2
- Q3 from pre-flight: NO retroactive change for installs that activated pre-1.11.2; their existing `shopos_restock_*` option values stay untouched

---

## Wave 2 — P0 features (each behind flag, each independently shippable)

**2.1 — ProductFeed multi-channel (Roadmap #3)**
- Only start this if a specific client has asked. Otherwise demote to P2.
- New submodule `ProductFeed/Channels/` with `Google_Xml`, `Facebook_Csv`, `Pinterest_Xml` implementing `Channel_Interface`
- Existing `/product-feed` endpoint keeps Google XML output unchanged
- New endpoints: `/product-feed/<channel>`
- Feature flag: `shopos_core_product_feed_multichannel_enabled`
- **Before extending Generator's surface**: read the header comment in `tests/snapshots/__fixtures__/wc_product_stub.php` — it lists which `\WC_Product` methods the snapshot stub already covers and which still need to be added (variable-product branch + `\WC_Product_Variation` stub) before variation XML can be snapshotted.

**2.2 — VariationSwatches migration + image swatches + tooltip + auto-color + card-image-swap (Roadmap #4)**

Master plan approved 2026-05-03 — see [/docs/archive/wave-2.2-master-plan.md](archive/wave-2.2-master-plan.md). The plan doc is the source of truth for cross-cutting concerns (hard rule #3 legacy/ coverage, decision dependencies, version-skew model, read-shim contract, feature-flag table). Sub-PR pre-flights cite anchors in the plan doc instead of re-litigating.

**Scope expansion call-out**: this wave was originally scoped as 3 sub-PRs (4a settings migration, 4b image swatches, 4c tooltip — per the audit). The approved master plan **expands to 6 sub-PRs**, adding 4d (auto-color sampler), 4e (auto-color fallback wiring), 4f (variation-image-on-card swap). 4d/4e/4f were not in the audit; they came from the 2026-05-03 pre-flight and are in some tension with §4.1 of the decisions doc (competitor-parity caution). The master plan §1 explains why they were approved despite §4.1 (cheap follow-ons on top of 4a's infrastructure; gated by flag, off by default).

Sub-PR ship order: **4a → 4f → 4b → 4c → 4d → 4e** (rationale in master plan §7). 4d and 4e share a flag — see master plan §6.

Sub-PR statuses (to be updated as each ships):

- **4a — Settings migration to Settings_Hub** — ✅ shipped 1.11.21 (2026-05-03) — flag `shopos_core_variation_swatches_settings_hub_enabled`
- **4f — Variation-image-on-card swap** — ✅ shipped 1.11.23 (2026-05-03) — flag `shopos_core_variation_swatches_card_image_swap_enabled`
- **4b — Image swatches** — ✅ shipped 1.11.24 (2026-05-03) — flag `shopos_core_variation_swatches_image_swatches_enabled`
- **4c — Tooltip on hover** — ✅ shipped 1.11.25 (2026-05-03) — flag `shopos_core_variation_swatches_tooltip_enabled`
- **4d — Auto-color sampler (pipeline + caching + scheduling)** — ✅ shipped 1.11.27 (2026-05-03) — flag `shopos_core_variation_swatches_auto_color_enabled` _(shared with 4e)_
- **4e — Auto-color fallback wiring (render path)** — ✅ shipped 1.11.28 (2026-05-03) — flag `shopos_core_variation_swatches_auto_color_enabled` _(shared with 4d)_
- **4g — Settings surface graduated** — ✅ shipped 1.11.45 (#TBD, 2026-05-11). Completes 4a: the ShopOS → Variation Swatches page is now the **sole** editing surface (the `settings_hub` flag is **retired** — `settings_schema()` always returns the 14 keys; `Settings_Reader` always prefers the new `shopos_core_variation_swatches_*` key with the legacy `shopos_vs_*` key as a permanent fallback per §4.5). The legacy WooCommerce → Settings → Products → "Shop swatches" section is **soft-deprecated** — kept registered (Hard Rule #2: the `?section=shopos_vs_shop_pick` URL keeps resolving) but renders only a one-line "moved" notice; WC's save pipeline no longer writes `shopos_vs_*` from there. **Hard Rule #3 `legacy/` edit** in `legacy/includes/class-settings.php` — approved 2026-05-11 for this wave (the `OPT_*` constants and the static read helpers `bool` / `max_visible` / `excluded_category_ids` / `should_apply_on_current_archive` are unchanged; only `add_settings()` changes). A version-gated re-sync migration at 1.11.45 (`Migrations::resync_variation_swatches_settings_from_legacy()`) copies the current `shopos_vs_*` values onto their new-namespace counterparts on sites where the flag was OFF (the default) — closing the post-1.11.21 drift the master plan's "P1 read model" had been guarding against — and is skipped on sites where the flag was ON (their new keys are already current). The `shopos_vs_*` option keys and the `shopos_core_variation_swatches_settings_hub_enabled` option are never deleted (the latter is now vestigial — read once by the migration, ignored thereafter). _(No new flag — this wave retires one. The legacy_settings_url shim repoints at the ShopOS page.)_
  - **Tests**: rewrote `tests/VariationSwatchesSettingsReaderTest.php` (dropped the flag-OFF/ON split — the shim is now unconditional); deleted `tests/VariationSwatchesSettingsHubSnapshotTest.php` (the flag-OFF "empty schema" contract no longer exists); added `tests/VariationSwatchesSettingsRelocationTest.php` (schema-unconditional, WC section is a moved-notice, section still registered, flag absent from registry, re-sync method + version/flag gating). `FeatureFlagsAdminTest`'s registry-completeness scan now skips `Core/Migrations.php` (migrations may read a retired flag's option to choose an upgrade path; that's not a runtime gate). Net suite: 389 tests / 1308 assertions.

**Wave 2.2 — ✅ shipped 1.11.28 (2026-05-03); 4g graduation 1.11.45 (2026-05-11)** — the 4e shipped-marker landed the parent per CLAUDE.md hard rule #9; 4g is a later additive sub-PR that closes the §4.5 migration of Roadmap item #4.

**2.3 — RestockNotify legacy migration (committed 2026-04-29)**

Parallels Wave 2.2's VariationSwatches migration. Required before the three deferred Wave-1.1 RestockNotify hooks (`should_inject`, `email_args`, `before_send`) can be added — call sites live in `legacy/includes/class-shopos-restock-*.php`, which hard rule #3 forbids editing without a migration plan.

Master plan (approved 2026-04-29) split execution into 3 sub-PRs (2.3a → 2.3b → 2.3c); the originally-proposed 2.3d (Ajax + Admin) is **skipped indefinitely** per Q-B since neither surface has any extension demand.

**2.3a** ✅ shipped 1.11.3 (#10, 2026-04-29) — modern `Subscribers` repository wrapping `\ShopOS_Restock_Database`. Pure groundwork (4-method static wrapper, no callers in this PR). Becomes canonical in 2.3b/c.

**2.3b** ✅ shipped 1.11.4 (#11, 2026-04-29) — modern `Email` + `Stock_Monitor` via `class_alias` swap in `Module::boot()`. Bilingual-email shell fix (4 Hebrew literals moved to `locales/<locale>.php` `shell_*` keys). 2 of the 3 deferred hooks land: `shopos_core/restock_notify/email_args` (filter) and `before_send` (action). The 3rd deferred hook (`should_inject`) waits for 2.3c.

**2.3c** ✅ shipped 1.11.5 (this PR, 2026-04-29) — modern `Frontend` via `class_alias` swap. Lands the final deferred Wave-1.1 hook `shopos_core/restock_notify/should_inject` (per-product render gate, distinct from `shopos_restock_should_enqueue`'s page-level asset gate) and moves 8 Hebrew literals into `locales/<locale>.php` (`js_*` for the `wp_localize_script` payload, `form_placeholder_*` for the inline form input placeholders). The 6-case `is_variation_truly_oos()` ladder copied verbatim from legacy with one unit test per branch. Browser-side JS-relocation parity for `footer_inject` is live-QA-only; PHPUnit locks the PHP-side output of all 4 injection paths.

Each sub-PR keeps `legacy/` files untouched; modern classes shadow via `class_alias`. Existing legacy filters (`shopos_restock_should_enqueue`), AJAX action (`shopos_restock_subscribe`), shortcode (`[restock_notify]`), unsubscribe URL pattern, transient cache key shape, asset handles, admin URLs, and the `{prefix}shopos_restock_subscribers` table are all preserved verbatim across the migration.

---

## Wave 3 — P1 functional improvements

Each item is its own PR with its own feature flag. Order within wave doesn't matter.

**3.1 — InfiniteScroll trigger modes (Roadmap #5) — expanded scope (committed 2026-04-29; rescoped 2026-05-04 per `/docs/archive/wave-3.1-master-plan.md`) — ✅ shipped (3.1a: 1.11.33 #38, 2026-05-04; 3.1b: 1.11.35 #TBD, 2026-05-10)**
- Trigger-mode setting: `auto` / `button` / `hybrid`. Concrete semantics in master plan §4-D1; "hybrid" = page-count threshold (UX pattern), distinct from the existing JS triple-stack trigger redundancy (engineering pattern).
- History API setting: `pushState` / `replaceState` / `disabled`. **Not net-new** — pushState already ships at `infinite-scroll.js:411-414`; this exposes existing behavior as configurable. Default `pushState` preserves current behavior byte-identically.
- Selector override via the new `selector` filter: replaces (or augments per master plan §4-D6) the 11-selector hardcoded priority list at `infinite-scroll.js:28-40`.
- Flag: `shopos_core_infinite_scroll_trigger_modes_enabled` (shared by 3.1a + 3.1b — precedent: 3.2a/b). Default off.
- **3.1a** ✅ shipped 1.11.33 (#38, 2026-05-04) — JS-only + settings: trigger_mode / history_mode / hybrid_threshold settings, JS dispatcher gates at `attachObserver` entry + post-`loadNext` threshold check, `applyHistoryMode` wrapper around the existing pushState call. No new hooks (those land in 3.1b). Flag introduced. Behind `shopos_core_infinite_scroll_trigger_modes_enabled` (default off); flag-OFF + flag-ON-default both byte-identical to pre-3.1a.
- **3.1b** ✅ shipped 1.11.35 (#TBD, 2026-05-10) — PHP wrapper render path + 4 deferred hooks + container_selector setting/filter + JS-side selector read.
- **Wave 3.1 known limitation**: Button-mode UI and hybrid post-threshold UI deferred — pick mode='auto' for full functionality. Button mode is functionally equivalent to max_pages=1; hybrid mode auto-loads up to threshold then halts. A future sub-PR or wave can add JS-side button render if a client need surfaces.
- **Folds in 3 hooks deferred from Wave 1.1**:
  - `shopos_core/infinite_scroll/selector` (filter) — replaces the hardcoded `.products` selector. Lands together with the JS-side read so the hook actually controls behavior.
  - `shopos_core/infinite_scroll/before_render` (action) — fires before the PHP-side render that this wave introduces (the module is JS-only today).
  - `shopos_core/infinite_scroll/after_render` (action) — fires after.
  - Each hook gets `@since` matching the version this wave ships in, plus a hook test asserting firing + payload.

**3.2 — Slider autoplay/loop/dots/lazy (Roadmap #6)** — ✅ shipped (3.2a: 1.11.29 #31, 2026-05-03; 3.2b: 1.11.30 #33, 2026-05-04)
- New Elementor controls: autoplay, autoplay delay, loop, pagination dots
- Add `loading="lazy"` to images beyond first viewport
- Flag: `shopos_core_sliders_advanced_controls_enabled` (introduced in 3.2a; reused — not redefined — by 3.2b)
- Split into 2 sub-PRs sharing the same flag (precedent: Wave 2.2 / 4d+4e):
  - **3.2a** ✅ shipped 1.11.29 (#31, 2026-05-03) — CategorySlider only. Indicator selector (`progress` / `dots` / `none`) supersedes the legacy `show_progress` switcher with a back-compat shim. Autoplay-wrap loop only (drag-past-end-wraps deliberately out of scope). Render path also gated on flag — rollback is byte-identical.
  - **3.2b** ✅ shipped 1.11.30 (#33, 2026-05-04) — ProductSlider only. Same controls + back-compat shim as 3.2a; advanced controls additionally gated on `display_mode = slider` (autoplay / loop / indicator have no meaning in grid mode), so grid-mode output is unchanged. Runtime is the shared `category-slider.js` engine from 3.2a — no JS edits needed.
- **Lazy-load line is a no-op for in-wave work** — CategorySlider uses CSS `background-image` (not `<img>`), so the HTML `loading="lazy"` attribute does not apply. ProductSlider's `<img>` markup already receives WP-core auto-lazy (since WP 5.5). Real lazy-loading on CategorySlider's CSS backgrounds, if ever wanted, would be a separate Wave 3.x or later item using `IntersectionObserver`. This explicit acknowledgement exists so a future auditor doesn't read the original roadmap line as unmet.

**3.3 — CheapestDefaultVariation strategy selector (Roadmap #9)** ✅ shipped 1.11.32 (#36, 2026-05-04)
- Setting: `cheapest` / `first_in_stock` (select). Default `cheapest` — flag-ON sites with default setting see byte-identical behavior to flag-OFF.
- Scope reduction from original audit: dropped `featured` (undefined for variations in WC core — variations have no `featured` flag; no client need surfaced) and `disabled` (redundant with the module's own enable toggle and with the existing `should_apply` filter returning false). Hard rule #2 not engaged — neither was ever shipped.
- Per-product opt-out: meta-only via post meta key `_shopos_cheapest_variation_strategy` (string in enum, or empty/missing = use global). No admin meta-box UI in this wave — a future wave can add the box non-breakingly if a client surfaces the need.
- New filter: `shopos_core/cheapest_variation/strategy` (`@since 1.11.32`). Resolution order: setting → meta override → filter override (filter is final word). Out-of-enum filter values fall back to pre-filter resolved value with a Logger warning.
- Flag: `shopos_core_cheapest_variation_strategy_enabled`. Flag-OFF preserves the current hardcoded cheapest path verbatim.

**3.4 — MyAccount endpoint extensibility (PROPOSED, NOT YET APPROVED)**
- Proposed during Wave 1.1 pre-flight to home the two deferred MyAccount hooks (`endpoints` filter, `sidebar_html` filter). MyAccount today is CSS-only — no PHP render path exists.
- Promote to committed only when an internal MyAccount extension need surfaces; until then this stays out of scope and may be dropped entirely.

---

## Wave 4 — P2 polish

**4.1 — RestockNotify CSV export + GDPR (Roadmap #10) — ✅ shipped 1.11.37 (4.1a) + 1.11.38 (4.1b); split per Wave 3.1 precedent**

- **4.1a** ✅ shipped 1.11.37 (#44, 2026-05-11) — WP_Privacy exporter + eraser registered under `shopos-core-restock-notify`. Exporter returns one item per matching subscription with all 9 column fields. Eraser nulls `customer_name`/`customer_email` (empty string — columns are NOT NULL) and flips `status` to `'unsubscribed'`; row preserved as audit trail. Unconditional — privacy hooks are a platform contract, not flag-gated (OS-5 decision call, 2026-05-11). Two new `Subscribers` methods (`find_by_email`, `erase_pii_by_email`) query `$wpdb` directly because no legacy `\ShopOS_Restock_Database` method offers exact-email lookup or PII null semantics, and Hard Rule #3 blocks adding to the legacy class.
- **4.1b** ✅ shipped 1.11.38 (#TBD, 2026-05-11) — `Export Subscribers` submenu under the legacy `restock-notify` parent (OS-1); admin-post.php form + `manage_woocommerce` capability check (cap first, nonce second per WP convention); CSV streams the full `shopos_restock_subscribers` table with UTF-8 BOM, comma delimiter, all 9 columns matching 4.1a Privacy exporter labels byte-for-byte, filename `restock-notify-subscribers-YYYY-MM-DD.csv`. Empty `notified_at` renders as empty cell; empty table emits headers-only CSV. Flag-gated behind `shopos_core_restock_notify_csv_export_enabled` (default off); defense-in-depth — flag-OFF means neither the submenu nor the `admin_post_*` listener attaches. `Subscribers::all()` reads the full table into memory; intended for expected merchant scale (≤ low tens of thousands), streaming variant deferred.

**4.2 — Slider design tokens as Elementor controls (Roadmap #11) — ✅ shipped 1.11.39 (#TBD, 2026-05-11); CategorySlider only**
- 4 color controls (`cs_bg_color` / `cs_ink_color` / `cs_mute_color` / `cs_line_color`) emit `--cs-bg|ink|mute|line` overrides on `{{WRAPPER}} .cs`; empty default → Elementor omits selector → `.cs` block's existing `oklch()` declarations remain (byte-identical pre-4.2 render).
- 3 arrow controls (`cs_arrow_size` px, `cs_arrow_radius` px/%, `cs_arrow_duration` ms) emit new `--cs-arrow-*` vars. CSS consumes them via `var(--cs-arrow-X, <hardcoded fallback>)` so unset → 40px / 50% / .18s (prior values byte-identical).
- No flag — purely additive. `--cs-accent` was already exposed before 4.2; this wave covers the 4 remaining color tokens + the 3 arrow values.
- ProductSlider scope deferred — roadmap line names `--cs-*` only. Future wave can mirror the pattern for `--ps-*` tokens if needed.

**4.3 — InfiniteScroll skeleton/fade tokens (Roadmap #12) — ✅ shipped 1.11.36 (#TBD, 2026-05-11)**
- 5 settings: `shimmer_base_color`, `shimmer_highlight_color`, `shimmer_duration_ms`, `fade_duration_ms`, `fade_transform_px`
- Emitted at runtime as `--shopos-ui-is-*` CSS custom properties on `:root` via `wp_add_inline_style` (uniform-shape Mechanism A from Wave 3.1b precedent)
- Existing hardcoded CSS values preserved as `var(--shopos-ui-is-X, fallback)` so flag-OFF / no-settings-saved is byte-identical to pre-4.3 render

---

### Off-roadmap waves (4.4 / 4.5 — VariationSwatches compatibility work)

Waves 4.4 and 4.5 were **not** drawn from the original 9-item P2 list. They came out of an early-May client-compat investigation (1.11.6–1.11.17 era) that ran on a separate branch lineage from Waves 4.1 / 4.2 / 4.3. They are recorded here so the 4.4/4.5 numbers are not silently floating; neither has shipped to main.

**4.4 — VariationSwatches preselect timing fix (DROPPED — kept on origin as artifact)**
- Scope intent: archive→PDP variation image race (Issue 3 from internal QA notes).
- Outcome: **dropped.** The original bug did not reproduce on staging; multiple fix attempts regressed Quick View. The investigation surfaced no reliable repro path. Branch `wave-4.4-preselect-timing-fix` is preserved on origin (per the explicit note in PR #17's body) as an artifact of the attempt; not merged, not intended for future merge.
- No version on main carries any 4.4 work.

**4.5 — VariationSwatches WPC FBT + Bundles compatibility (✅ shipped 1.11.40)**
- Scope: forward all `serializeArray()` form fields through to WC's standard `wc-ajax=add_to_cart` endpoint so WPC Product Bundles (`woosb-ids-*`) and WPC FBT (`woobt_ids`) hidden inputs reach the cart. Sister work to the dropped 4.4 attempt.
- **Approved single-line `legacy/` edit** in `templates/variation-buy-box.php` — adds `do_action( 'woocommerce_before_add_to_cart_button' )` inside `.shopos-actions` so WPC FBT's `woobt_ids` injection point exists. Standard WC hook; no-op on sites without compat plugins. Hard Rule #3 exception explicitly framed in PR #17's body and in this entry.
- Flag `shopos_core_variation_swatches_bundle_compat_enabled` (default off) gates the JS payload-build branch, the `window.ShopOSCoreVSFlags = { bundleCompat: <bool> }` global, and the `woobt_added_to_cart` → `wc_fragment_refresh` document-body bridge. The template `do_action` is unconditional (Hard Rule #1 additive exception — purely a new injection point, no ShopOS-shipped listener).
- PHP-side plumbing: new `Module::inject_feature_flags()` on `wp_enqueue_scripts` priority 10001, emitting the JS global before the `shopos-core` script handle.
- **PR history**: PR #17 (1.11.17 era) carried the design + staging-validated QA against WPC FBT + FunnelKit Cart but never merged — by 1.11.39 it was 22+ version bumps stale and the JS/template/Module diffs would have regressed Waves 2.2/4a, 4b, 4c, 4d, 4e. Re-shipped on a fresh branch from current main, extracting only the bundle-compat additions; PR #17 closed with a link to the v2 PR. The stale `wave-4.5-bundle-fbt-compat` branch is preserved on origin as an artifact (same pattern as Wave 4.4's `wave-4.4-preselect-timing-fix`).
- **Test coverage**: 4 new PHPUnit tests in `tests/VariationSwatchesBundleCompatTest.php` (one in an isolated process to bypass `ShopOS_VS_Plugin` pre-loading) covering the inject_feature_flags() plumbing; +4 reported tests / +8 reported assertions. JS behavior changes (full-form serialize + woobt bridge) remain staging-validated per PR #17, as PHPUnit cannot exercise them.

---

## Wave 5 — Admin & operability

Not from the original 9-item audit list. Operability work surfaced from day-to-day use of the suite.

**5.1 — Feature Flags admin page — ✅ shipped 1.11.44 (#TBD, 2026-05-11)**
- New `ShopOS → Feature Flags` submenu: one checkbox per flag, grouped by module, each with a one-line "what this gates" description, the equivalent `wp option update …` line, the introducing version, and a "shared switch" marker where one flag drives several sub-features. Saving posts to `admin-post.php` (`shopos_save_feature_flags`, nonce + `manage_woocommerce`), writing each registry flag to `1`/`0`.
- New `Feature_Flags::registry()` — canonical list of all 11 flags (`tools/settings_import`, `variation_swatches/{settings_hub, card_image_swap, image_swatches, tooltip, auto_color, bundle_compat}`, `sliders/advanced_controls`, `cheapest_variation/strategy`, `infinite_scroll/trigger_modes`, `restock_notify/csv_export`) — and `Feature_Flags::option_name()` / `Feature_Flags::is_forced_by_filter()` helpers. The registry is the single source of truth shared by the admin page and the `/docs/feature-flags.md` table; a test asserts it stays in sync with the `is_enabled()` call sites in `src/` (no missing or stale entries).
- A flag whose effective state is forced by a `shopos_core/feature_flag/{module}/{feature}` filter renders its checkbox **disabled** with a "forced by code" note — the DB option can't override a filter, so the UI says so rather than lying. Such flags are also skipped by the save handler.
- **No feature flag** — this is infra (a new admin submenu, writes only on user action), same class as the unflagged Dashboard / Tools pages; Hard Rule #1's additive exception applies. Also fixed the stale `/docs/feature-flags.md` "Active flags" table (added the missing 3.1 / 3.2 / 3.3 / 4.1b rows; corrected the auto-color "4e (pending)" marker to 1.11.28) and added an "Admin page" + registry-is-canonical note there — stated separately in the PR description per "decisions separate from code".
- **Test coverage**: 8 new PHPUnit tests in `tests/FeatureFlagsAdminTest.php` (registry⇄source completeness both directions, registry-entry shape, no duplicates, `option_name()` format, `is_forced_by_filter()` true/false, hub registers the save handler); +8 reported tests / +239 reported assertions (the assertion count is loop-heavy — several tests iterate the 11-entry registry). Added a `has_filter` / `has_action` smart stub to `tests/bootstrap.php` backed by the existing hook registry.

---

## Wave 6 — Shop Filters (new module — faceted AJAX product filters)

Net-new epic (not from the original audit). Replaces the third-party "Filter Everything Pro" plugin, which renders poorly on this Elementor store (the "shop grid blowout" the theme stopgaps in 1.11.24). Approved 2026-05-20 — see `/docs/decisions-2026-04-28.md` §5 (2026-05-20 addendum) for the schema-change sign-off, indexing approach, and filtered-URL SEO stance. Justified despite §4.1's competitor-parity caution because it serves a concrete internal need (the suite owner relies on shop filtering today and the current third-party tool is failing); internal-only, opinionated-by-default per §4.1.

New module `src/Modules/ShopFilters/` (namespace `ShopOS\Core\Modules\ShopFilters`), **disabled by default** and additionally gated per phase by `shopos_core_shop_filters_<feature>_enabled` flags (default off) — purely additive and reversible; touches no other module or theme core. Carries the suite's first module-owned index table (`{prefix}shopos_shop_filter_index`), auto-installed via `Migrations::run()` (dbDelta), dropped on uninstall. Admin-AJAX per §4.4 (no REST). Catalog scale: medium (~1k–10k). Versioning: **1.12.0** for Phase 6.1, then 1.12.x per phase (minor bump for a new module; not a major bump).

Sub-PR phases (each ≤12 files, ≤3 modules, independently shippable, flag-gated; each code phase marks its own bullet `✅ shipped <version> (#PR, date)`, bumps the Last-updated line, and — when it adds tests — the CLAUDE.md PHPUnit count):

- **6.0 — Roadmap + decision record** — docs only (this entry + the §5 decisions addendum). No code, no version bump. Kept separate from code per "decisions separate from code".
- **6.1 — Foundations** ✅ shipped 1.12.0 (2026-05-20) — module skeleton + `Database` index table (the schema change) + event-driven `Indexer` (dirty-queue + reconcile sweep; Action-Scheduler-or-wp-cron) + `Term_Helpers` (duplicated pure swatch/in-stock helpers, decoupled from VariationSwatches) + admin "Reindex now" tool. No storefront output. Flag `shopos_core_shop_filters_indexer_enabled`. **1.12.2 bug-fix** (caught in review before QA): non-variation global attributes on variable products now follow overall stock instead of being wrongly variation-gated (which would have hidden them under in-stock-only filtering). **1.12.3** (by request): the ShopOS → Shop Filters page now carries the indexing toggle, live index status (products / rows / last sweep / scheduled) and the reindex tool — all manageable from wp-admin without WP-CLI. The toggle is a `settings_schema` checkbox writing the same option the feature flag reads. **1.12.4** bug-fix (found in QA): the recurring reconcile sweep now schedules correctly — `ensure_scheduled()` was running on `plugins_loaded` before Action Scheduler's store is ready (silent no-op); deferred to `init`. **1.12.5** (QA polish): the status line shows the actual last-refresh time (not the internal resume watermark, which read as a confusing old date during catch-up); a full reindex parks the watermark at "now" so the sweep doesn't re-churn; the catch-up chain uses Action Scheduler; an on-page note appears when `DISABLE_WP_CRON` is set.
- **6.2 — Facet engine** ✅ shipped 1.12.1 (2026-05-20) — pure `Facet_Engine` / `Category_Tree` / `Url_State` / `Facet_Config`: AND-across / OR-within with self-exclusion, hide-zero (req #2), hide-empty-facet (req #1), category hierarchy (req #3). No flag (dormant helper classes — Hard Rule #1 additive exception). Hooks `shopos_core/shop_filters/{facet_config, is_facet_visible}`. **Scope drift:** `Query_Builder` (the index→facet-engine `$wpdb` glue) moved to 6.3a, where it's wired to the AJAX endpoint and integration-tested rather than shipped dormant.
- **6.3a — Frontend read path** ✅ shipped 1.12.6 (2026-05-20) — `Query_Builder` (index → facet-engine glue, moved from 6.2) + `[shopos_shop_filters]` shortcode (server-renders the initial facet tree) + public admin-AJAX query endpoint `shopos_core_shop_filters_query` (nonce + rate-limited) + JS grid-swap (reuses InfiniteScroll container detection; fetches the filtered front-end URL for Elementor markup parity + IS coexistence) + checkbox facets. Flag `shopos_core_shop_filters_frontend_enabled` (default off; gates both shortcode output and the public endpoint). **Scope trim:** the `default_orderby` / `products_per_page` / `in_stock_only_default` option keys the plan pencilled in for 3a are deferred — the grid is the filtered front-end URL, so WooCommerce owns pagination/orderby/per-page; revisit in 6.5b. No render hooks here (those are 6.3b). **1.12.7 (6.3a.1) read-path fix** (found in QA): 6.3a never actually filtered the grid — there was no server-side bridge and our `filter_pa_*` params are intentionally distinct from WC's native `filter_<attr>`. Added `Query` (a `woocommerce_product_query_tax_query` filter for shop/category/attribute archives + a scoped `pre_get_posts` for product search) so a plain load filters the main query, and switched the front-end controller from AJAX grid-swap to full **reload** navigation — selection now persists in the URL, sort keeps the filters, search pages work, and Infinite Scroll runs normally on the reloaded page. Chips are server-rendered; the public AJAX endpoint is retained (Hard Rule #2) but no longer called by the bundled JS. Known deferral: facet *counts* on a search page still reflect the whole catalogue (the index has no full-text search subset) — a later refinement. **1.12.8 (6.3a.2) stock-visibility parity** (found in QA — a facet value showed "(1)" but the filtered URL returned 0 products): the facet base + counts now mirror the storefront grid when the store hides out-of-stock items (`woocommerce_hide_out_of_stock_items`), so a value backed only by a hidden out-of-stock product no longer shows a phantom count; counts are by product-level presence within the in-stock base, matching WC's term matching. **1.12.9 (6.3a.3) index diagnostic** (QA aid): a read-only table on the ShopOS → Shop Filters page (gated by the indexer flag, beside the reindex tool) listing each attribute term's slug / name / indexed product + in-stock counts straight from the index — to debug storefront *data* problems (scrambled term name↔slug pairs, orphaned index terms, all-out-of-stock values) that make a count disagree with the grid or make one size return another. Reproduced by a third-party filter plugin too — but the diagnostic showed the data was fine, which pointed the finger back at our code (see 1.12.10). **1.12.10 (6.3a.4) hyphen + pagination fix** (the actual blank-grid root cause): `Url_State::sanitize_taxonomy` stripped hyphens, mangling hyphenated attribute taxonomies (`pa_shoe-size` → `pa_shoesize`) so the `tax_query` matched nothing — every hyphenated-attribute (i.e. size) filter returned blank; hyphens are now allowed. Also: applying a filter from a `/page/N/` URL kept the pretty-pagination path and could 404 on a smaller filtered set — the controller now resets the `/page/N/` segment to page 1. (This re-frames 1.12.8: the grid was empty because of the mangled taxonomy, not OOS; the stock-parity fix stays correct but wasn't the grid fix.)
- **6.3b — Facet UI** ✅ shipped 1.12.11 (2026-05-20) — the deferred storefront UI from 6.3a. Category-tree facet renders on top as a pruned, count-rolled-up parent → child hierarchy (`Category_Tree` wired into `Query_Builder`); **design decision: category nodes navigate to their own archive (`get_term_link`), not a `filter_product_cat=` param** — categories are navigation on this store. Attribute facets whose terms carry VariationSwatches term meta render as colour/image swatches, read directly via `get_term_meta` (`shopos_swatch_color` / `shopos_core_variation_swatches_term_image_id`) — no call into the VariationSwatches module (decisions §5.7); the underlying checkbox is preserved so the reload transport is unchanged. New stylesheet (removes the default list bullets) + mobile bottom-sheet drawer: focus-trapped, ESC/overlay close, `prefers-reduced-motion`, and **defer-until-Apply on mobile** (ticking collects the selection; only "Apply" navigates — the standard drawer pattern, vs desktop's navigate-on-tick). **Hook-name scope change:** the planned `.../grid_html` filter has no grid to filter under the 6.3a.1 reload transport (WooCommerce renders the grid on reload), so it ships as `shopos_core/shop_filters/panel_html` (filters the returned panel markup) alongside the `before_render` / `after_render` actions. Reuses `frontend_enabled` — no new flag.
- **6.3c — Storefront string labels** ✅ shipped 1.12.12 (2026-05-20) — by request: every user-facing string in the filter panel (mobile toggle, drawer title/aria, Close, Categories heading + aria, Clear all, Apply, Clear, and the singular/plural result-count line) is now an editable `text` field on the ShopOS → Shop Filters settings page. A new `Labels` class is the single source of truth (key → admin-label + English default); `settings_schema()` builds the fields from it and the templates resolve through `Labels::get()`. **Additive, no flag** (Hard Rule #1 exception): a blank field falls back to the English default, so output is byte-identical until the owner types an override — which is how the site sets Hebrew wording without code (resolves the 6.3b i18n open question, §4.2).
- **6.5b — Price facet + sort** ✅ shipped 1.12.13 (2026-05-20) — built **ahead of 6.4 / 6.5a** by request, so it took the next sequential version (1.12.13), not its originally-pencilled 1.12.15. Price renders on top as OR-checkbox **bands** (a `price_bands` setting of comma-separated upper bounds, or auto-derived rounded bands when blank); band counts + storefront grid filtering read min/max from `wc_product_meta_lookup` (§5.2 — never duplicated) via a `posts_clauses` overlap join, carried in a dedicated `filter_price=min-max,min-` URL param. A **Sort by** dropdown sets `?orderby` (WooCommerce honours it on reload) and a **default-sort** setting drives `woocommerce_default_catalog_orderby` site-wide. Reuses `frontend_enabled`. **Scope note:** the original 6.5b also listed **on-sale / in-stock** facets — those are **deferred** (not yet built); revisit as a later 6.5c if wanted. v1 approximation: term-facet counts don't subtract an active price selection (price lives outside the term engine); grid + band counts stay correct.
- **1.12.14 in-stock-filtering bug-fix** (found in QA) — filtering an attribute returned products whose selected value was sold out: WooCommerce assigns a variable product *all* its variation option terms, so the grid's `tax_query` matched a parent even when that size's variations were out of stock, and the counts mirrored it (`postings_for_products(..., false)`). Now both read the index's per-variation in-stock truth — counts use `postings_for_products($base, $hide_oos)` and the grid is constrained to the index's in-stock product set via `post__in` (`Query::apply_instock_post_in`, gated on the index having rows; the `tax_query` stays as the fallback when indexing is off). Realigns the read path with requirement #2. Keys off `woocommerce_hide_out_of_stock_items` (enabled on the live store).
- **1.12.15 UI polish** (by request) — filter-panel buttons (mobile toggle, active-filter chips, clear/clear-all, Apply, sort `<select>`) restyled more minimal + rounded, referencing the theme's `--shopos-ui-*` design tokens (`--shopos-ui-radius-pill`, `--shopos-ui-radius-md`, `--shopos-ui-button-font-weight`, `--shopos-ui-button-tracking`) with fallbacks so they harmonise with the store. CSS-only (`shop-filters.css`, cache-busted by `SHOPOS_CORE_VERSION`).
- **1.12.16 button restyle follow-up** — the 1.12.15 core button CSS was being overridden by the theme/Elementor on the live store (so no visible change). Instead of fighting specificity, the panel toggle / Apply / Clear / sort now carry the theme's own `.shopos-ui-btn` (+ `--ghost` / `--block` / `--link`) and `.shopos-ui-select` primitives, so they render identically to the site's buttons (and inherit RTL handling). Template-only; the core `shop-filters.css` button styles remain as the no-theme fallback.
- **1.12.17 search-results faceting** (urgent, by request; resolves the 6.3a.1 deferral) — on a product-search page (`?s=…&post_type=product`) the panel now facets over the products matching the query instead of the whole catalogue. `Shortcode` passes `get_search_query()` into the request; `Query_Builder` seeds the base universe from an unpaginated product search (`search_product_ids()`) intersected with the index via the new `Index_Repository::filter_indexed()` (in-stock-aware when the store hides OOS). Facets, counts, category tree and price bands all follow. Selecting a filter already worked on search (the `Query` bridge); this fixes the *panel relevance*. **Caveat:** the base uses a generic product `WP_Query` `s=` search — a search plugin (e.g. Advanced Woo Search / `type_aws`) that only refines the *main* query may yield a slightly different set than its own results; revisit with an AWS-specific integration if the panel and grid diverge.
- **1.12.18 AWS search-grid enforcement** (urgent follow-up to 1.12.17) — on the live store the search-results grid is supplied by **Advanced Woo Search** (`type_aws=true`), which bypasses our query-level `post__in` / `tax_query`, so a product whose only in-stock size was outside the selection still showed (the bug was gone on category pages, present only on search). Added a `the_posts` safety net (`Query::enforce_filters_on_search` + pure `filter_posts_to_ids`) that drops any product not in the index's in-stock set for the active selection — scoped to the front-end main *search* product query, only when filters are active and the index has rows. Works regardless of how the search plugin builds the list (SQL or short-circuit), as long as the grid uses the main query loop. Known limitation: post-filtering can leave a search page slightly under-filled vs its reported count (pagination is computed pre-filter) — acceptable for correctness; revisit if it bites.
- **1.12.19 desktop-toggle fix** — the mobile "Filter" toggle was leaking onto desktop: after 1.12.16 the toggle carries the theme's `.shopos-ui-btn` (`display:inline-flex`, 0,2,0), which beat the plain `display:none`. The desktop hide + mobile show are now `!important` so they win. CSS-only.
- **6.4 — Admin facet config** ✅ shipped 1.12.21 (2026-05-20) — per-attribute matrix on `shopos_core/module_page/shop_filters`: show/hide a filter, reorder it, and hide it on chosen categories (a fetched product-category multiselect). Writes the `shopos_core_shop_filters_facet_config` option `Facet_Config` already reads; the read is now gated by the new flag `shopos_core_shop_filters_admin_config_enabled` (default off) so flag-off fully reverts any saved config to the auto-derived defaults. Save via `admin_post_shopos_core_shop_filters_save_facet_config` → pure `Facet_Config::sanitize()`. **Scope trim:** the pencilled per-facet **type** selector was dropped — on this storefront type is auto-derived (categories = navigation tree, attributes = checkbox auto-upgrading to swatch when term meta exists), so a type control would change nothing; the matrix exposes only show / order / hide-on-category. No JS (plain POST form), no new hooks (deferred the speculative `admin_facet_types` filter).
- **6.5a — SEO policy** ✅ shipped 1.12.22 (2026-05-20) — filtered shop / category / search URLs get `noindex,follow` + a canonical to the clean archive (query-string params only; no rewrite rules). Routes through whichever SEO plugin is active — RankMath (`rank_math/frontend/{canonical,robots}`), SEOPress (`seopress_titles_{canonical,robots}`), Yoast (`wpseo_{canonical,robots_array}`) — or WordPress core (`wp_robots` + a `wp_head` canonical link) when none is present, so it never emits a second tag alongside the plugin. Canonical override strips filter/sort/pagination from the plugin's own canonical (fixes RankMath's query-string self-canonical on filtered URLs). "Filtered" = facet / price / on-sale / in-stock selections only — sort-only and pagination-only URLs are left untouched. Search = noindex only (no canonical rewrite). Flag `shopos_core_shop_filters_seo_policy_enabled` (default off), independent of `frontend`. Pure seams (`is_filtered_state` / `clean_url` / robots mutations) unit-tested; plugin routing live-QA. **Deferred hooks:** the pencilled `.../canonical_url` + `.../robots` filters were not added (the flag is the control).
- **6.5c — On-sale / in-stock facets** ✅ shipped 1.12.23 (2026-05-20) — the remaining `wc_product_meta_lookup`-sourced numeric facets (§5.2 — never duplicated). An **On sale** checkbox renders in the panel whenever the context has sale products (hide-zero); an **In stock** checkbox renders only when the store shows out-of-stock items (`woocommerce_hide_out_of_stock_items` off) — otherwise it's a no-op (the grid is already in-stock-only) so it's omitted. Carried in top-level `onsale=1` / `in_stock=1` URL params (the convention `Url_State` already parsed); grid filtered via a `posts_clauses` overlap on the price facet's lookup join (`Query::flags_where_sql`), counts + grid-set via `Query_Builder::{filter_by_flags,shape_flag_facet}`. Three editable `Labels` strings (Availability heading, On sale, In stock). Reuses `frontend_enabled` — no new flag. **Wave 6 epic complete.**

- **6.6 — Module graduated to always-on** ✅ shipped 1.12.26 (2026-06-09) — owner directed Shop Filters to ship on by default with **no feature flags**. All four `shop_filters` flags (`indexer` / `frontend` / `seo_policy` / `admin_config`) **hard-removed** (gates + `Feature_Flags::registry()` entries + the `indexer_enabled` settings toggle deleted); the module is unconditionally active when the module itself is enabled. The Index diagnostic admin surface (`Diagnostics`) was removed. Deliberate owner-approved override of Hard Rules #1 (no-flag), #2 (option-key sunset — flag keys now inert/orphaned, not migrated; supersedes §5.6's opt-out-via-flag), and #4 / the 12-file ceiling (graduation + diagnostic removal in one PR). **No option-based rollback** — disable via the modules registry or revert the release. See decisions §5.8.

> **RESOLVED — AWS search grid (1.12.20):** on a search page the grid responds to our `tax_query` (it IS the main query), but sold-out-size products survived because Advanced Woo Search re-asserts its own result list on `the_posts` *after* us. Moving our `the_posts` enforcement (`Query::enforce_filters_on_search`) to priority `99999` so it runs last fixed it (QA-confirmed). Together with 1.12.17 (search-scoped facets), search now filters correctly end-to-end.

---

## Wave 7 — Quick View (new module) + ProductSlider grid parity

Re-opens audit item A2 (Quick View), dropped per §4.1 and re-opened 2026-06-11 by owner request — the first revisit trigger. See `/docs/decisions-2026-04-28.md` §6 for the planning-time decisions (two-stage scope, WC-loop-hook trigger surface, admin-AJAX transport, inline-end drawer, flag, editable labels, swatches coupling stance).

Two stages: first the ShopOS ProductSlider **grid mode** is parity-audited against Elementor Pro's Products widget and approved gaps fixed (the quick-view icon must behave identically on both grids), then the new module `src/Modules/QuickView/` ships — a per-card icon opening a slide-in drawer (inline-end edge = left on this RTL store) with gallery image, title, price, short description, SKU / categories / stock, the standard add-to-cart surface (VariationSwatches lights up via its existing hooks), and a "View full details" PDP link. Flag `shopos_core_quick_view_frontend_enabled` (default off). Versioning: parity fixes are 1.12.x patches; the module is a minor bump (**1.13.0**).

Sub-PR phases (each ≤12 files, ≤3 modules, independently shippable; each marks its own bullet `✅ shipped <version> (#PR, date)` and bumps the Last-updated line + CLAUDE.md PHPUnit count when tests are added):

- **7.0 — Roadmap + decision record** ✅ shipped (docs only, 2026-06-11) — this entry + the §6 decisions addendum. No code, no version bump.
- **7.1 — ProductSlider grid parity audit** ✅ shipped (docs only, 2026-06-11) — findings doc `/docs/archive/grid-parity-audit-2026-06-11.md`: card-level parity already met by construction (shared `content-product` render path); two container-level defects found (G1 fractional-mobile-columns invalid CSS, G2 theme mobile-columns Customizer rule hijacking `.cs-*` containers on archives); six intentional divergences documented as no-fix (fixed image height, no pagination, sale-flash/hover design language, gap token); InfiniteScroll generic-fallback selector noted as watch-list (no live exposure).
  - **7.1a — G1 fix** ✅ shipped core 1.12.31 (2026-06-11) — grid mode rounds `per_view_mobile` to a whole column count before emitting `--cs-per-mobile` (`repeat()` rejects fractions → the mobile grid collapsed to one column); slider mode keeps the float for the peek effect. +3 reported tests / +3 assertions (`ProductSliderGridMobileColumnsTest`).
  - **7.1b — G2 fix** ✅ shipped theme 1.11.26 (2026-06-11) — the `shopos_shop_cols_mobile` inline rule now carries `:not(.cs-track):not(.cs-grid)` so ShopOS slider/grid widget containers on product archives are excluded from the forced `display:grid !important` override; main archive grid behaviour unchanged.
- **7.2 — QuickView module** ✅ shipped 1.13.0 (2026-06-11) — new module `src/Modules/QuickView/`: `Module` (settings_schema from `Labels`, flag-gated boot) / `Frontend` (trigger button on `woocommerce_after_shop_loop_item` prio 15 — a real `<button>` child of the loop `<li>`, absolutely positioned over the image's top inline-end corner, WooSQ placement convention; assets; footer drawer shell printed only on pages that rendered a trigger) / `Ajax` (`shopos_core_quick_view_product`, nonce + 30/60s per-IP rate limit, serves only published catalog-visible products) / theme-overridable `templates/drawer-content.php` (renders the standard `woocommerce_single_product_summary` stack — VariationSwatches' buy-box swap + price de-dup light up unaided — plus main image and a "View full details" PDP link) / `quick-view.{js,css}` (focus-trapped inline-end drawer — left on RTL — ESC/overlay close, `prefers-reduced-motion`, per-product response cache, no-JS click falls through to nothing — the PDP link on the card itself stays the canonical path). Six editable `Labels` strings (trigger aria, drawer title, close, details link, loading, error). New hooks: `shopos_core/quick_view/show_trigger` + `shopos_core/quick_view/drawer_html`; JS event `shopos_core_quick_view_loaded` (jQuery + native), added additively to `shopos-swatches.js`'s quick-view re-init listener list (§6.7; the MutationObserver fallback would catch the injected buy box regardless). Flag `shopos_core_quick_view_frontend_enabled` (default off) — flag-off boots nothing. +17 reported tests / +89 reported assertions (`QuickViewModuleTest`, `QuickViewFrontendTest`, `QuickViewAjaxTest`; the assertion jump includes existing registry-iterating tests — `FeatureFlagsAdminTest`, `ModuleRegistryTest` — scaling with the new flag + module). Rollback: `wp option update shopos_core_quick_view_frontend_enabled 0` (module also absent from `shopos_core_modules` on existing installs until enabled on the ShopOS dashboard).
- **7.3 — Wave 7 QA follow-ups** ✅ shipped 1.14.0 (2026-06-18) — owner live-QA fixes: (a) **ProductSlider as archive grid** — `current_query` source in **grid mode** now renders the full archive page (no longer truncated by the widget "Max products" cap, which only WooCommerce's products-per-page bounds) and emits `paginate_links()` pagination below the grid (built from `$wp_query->max_num_pages`, so it works on an Elementor archive template that never ran `wc_setup_loop()`); slider-mode `current_query` still honours the cap (paginating a draggable row is meaningless); cap raised 24→48 for the other sources. (b) **Quick View drawer full-width** — neutralised WooCommerce's single-product `div.product` two-column float (`.images`/`.summary` at 48%) inside `.shopos-quick-view__body` so the title/price/variations/add-to-cart stretch full width. (c) **Quick View icon restyle** — black glyph on a solid white circle, pinned to the physical right corner (uses `right`, not logical `inset-inline-end`), always visible, hover is a small scale pop only (no colour/background shift). No new tests (CSS + archive-query branch; harness can't exercise `$wp_query`/`is_*` archive conditionals cleanly); suite stays 518 / 1734. Note: **image-swatch card image swap is unrelated** — it is correct but gated behind `shopos_core_variation_swatches_card_image_swap_enabled` (default off); enable that flag (ShopOS → Feature Flags, or `wp option update shopos_core_variation_swatches_card_image_swap_enabled 1`) to activate it.

---

## Wave 8 — Search (new module — in-house full-text product search, AWS replacement)

Owner request (2026-06-22): build an in-house product search engine to eventually drop the paid **Advanced Woo Search (AWS)** plugin. Same destination, staged across four flagged sub-PRs — AWS stays authoritative until parity is measured, then is deactivated last. Engine is **MySQL FULLTEXT** (`MATCH ... AGAINST`) over one denormalised `search_text` blob, mirroring the Shop Filters index machinery; stemming / fuzzy / synonyms / suggestions are AWS's moat and deliberately **out of scope**. Plan: `/Users/freemansmain/.claude/plans/here-s-some-broad-guidelines-giggly-stardust.md`. Each sub-PR ≤12 files, ≤3 modules, independently shippable, behind its own flag.

- **8.1 — Foundation: index + indexer** ✅ shipped 1.18.0 (2026-06-22) — new module `src/Modules/Search/`: `Database` (the `shopos_search_index` FULLTEXT table — one row per product: `product_id`/`sku`/`title`/`search_text`/`in_stock`, `ft_search`+`ft_title` FULLTEXT indexes, InnoDB; auto-installed by `Migrations::run()` on the version bump) / `Query_Engine` (pure `build_search_text()` blob assembly — strip-tags + whitespace-collapse, no WP) / `Search_Repository` (write path — `upsert`/`delete_product`/`count_indexed_products`/`clear_all`) / `Indexer` (Shop-Filters-style dirty queue + 30s debounced drain + 5-min reconcile sweep + offset-paged batch; `is_indexable` includes catalog-visibility `visible`+`search`, excluding `catalog`-only, matching WC search semantics) / `Admin_Page` ("Reindex all" batch tool under ShopOS → Search). Flag `shopos_core_search_indexer_enabled` (default off → zero hooks, no index maintenance; storefront unchanged). +14 reported tests / +71 reported assertions (`SearchDatabaseTest`, `SearchQueryEngineTest`, `SearchIndexerTest`, `SearchModuleTest`). Read path (MATCH...AGAINST score/where) deferred to 8.2 where `search()` actually calls it. Rollback: `wp option update shopos_core_search_indexer_enabled 0` (module also absent from `shopos_core_modules` until enabled on the dashboard).
- **8.2 — Live AJAX dropdown** ✅ shipped 1.19.0 (2026-06-22) — read path + storefront dropdown. `Query_Engine::search_sql`/`search_args` (ranked MATCH...AGAINST: broad FULLTEXT + 4× title boost + 1000/50 exact/prefix-SKU; WHERE OR-group pairs FULLTEXT with SKU exact/prefix + `search_text LIKE` substring — the fallback that rescues short / non-Latin tokens FULLTEXT's min-token-size drops); `Search_Repository::search()` (blank-term guard → []; null-on-empty-index deferred to 8.3 where the native fallback consumes it); `Ajax` (public `shopos_core_search_query`, nonce + 30/60s rate-limit, returns `[{id,title,url,price_html,image}]` with server-rendered price); `Frontend` (enqueue + `ShopOSSearch` localize); `search.js` (debounced + `AbortController`, combobox a11y — roles / `aria-activedescendant` / arrow-enter-esc / `aria-live`, "see all results" row, progressive enhancement so native form still submits JS-off) + `search.css`. Three settings (field selector default `input[type="search"], input[name="s"]`, min chars 2, debounce 200ms). Flag `shopos_core_search_dropdown_enabled` (default off, independent of the indexer flag). +8 reported tests / +61 reported assertions. Rollback: `wp option update shopos_core_search_dropdown_enabled 0`.
  - **8.2a — `[shopos_search]` shortcode** ✅ shipped 1.19.1 (2026-06-22) — owner request for a drop-anywhere search box. `Frontend::render_form` prints a native GET form (`input[type=search][name=s]` + hidden `post_type=product`) so the default dropdown selector auto-enhances it with no config and JS-off submits a normal product search; `placeholder`/`button` attrs. Gated by the same dropdown flag (registered in `Frontend::register`). +2 tests / +5 assertions.
- **8.3 — Results page** ✅ shipped 1.20.0 (2026-06-22) — the engine drives the native (theme-styled) product search-results grid. `Results_Query` hooks `pre_get_posts` at prio 5 (front-end + main + `is_search()` + product): sets `post__in` = engine-ranked ids + `orderby=post__in`, neutralises WP's native `s` LIKE via `posts_search` (keeping `s` set so `is_search()` stays true and Shop Filters' search-page logic still engages), and falls back to native WP search when the index is empty (`Search_Repository::has_data()` — the null-on-empty contract deferred from 8.2). Coexistence: Shop Filters' `apply_instock_post_in()` (prio 20) intersects its facet filter into the existing `post__in` while preserving the first array's order, so facets narrow within results and relevance order survives — no clobbering. One additive `shopos_core/shop_filters/search_product_ids` filter in ShopFilters `Query_Builder::search_product_ids()` feeds the facet panel's base universe the same engine ids (no hook removal / deprecation). Flag `shopos_core_search_results_enabled` (default off — the pre-cutover staging switch). +11 reported tests / +37 reported assertions (pure gating/id-planning seams; the WP_Query/$wpdb adapters are integration/live-QA). Rollback: `wp option update shopos_core_search_results_enabled 0`.
  - **8.3a — results-page live-QA fixes** ✅ shipped 1.20.1 (2026-06-22) — owner QA (AWS now deactivated) surfaced a blank results grid + empty facets: the results page + facet-feed call `search($term, -1)`, which built invalid `LIMIT -1` → empty result → grid `[0]` + empty facet base. `Search_Repository::search()` now clamps `limit <= 0` to an effectively-infinite cap. (Dropdown was unaffected — it uses `LIMIT 8`; the bug was masked until AWS, which had supplied the grid, was turned off.) Also removed the `field_selector` setting — owner uses `[shopos_search]` only, so the dropdown selector is the hardcoded `input[type=search], input[name=s]` the shortcode field matches. No net test change (two tests re-pointed).
- **8.4 — Graduation** ✅ shipped 1.21.0 (2026-06-22) — **scope changed from the original "parity tooling + cutover".** During 8.3 QA the owner deactivated Advanced Woo Search and confirmed the engine live (dropdown + shortcode + results working post-1.20.1 fix), obsoleting the parity-diff-vs-AWS plan (nothing to compare against) and the cutover flag (results-flag + AWS-off already = engine authoritative). Per owner decision the module **graduated to always-on** (owner-approved override of Hard Rule #1/#2, like Shop Filters 6.6): hard-removed the three `search` flags (`indexer`/`dropdown`/`results`) + registry entries, so `Module::boot()` wires every surface whenever the module is enabled — the module-enable toggle is the single kill-switch, and a not-yet-built index degrades safely (`has_data()` → dropdown empty + results fall back to native WP search). Shop Filters' AWS safety-net hooks stay (harmless no-op, no removal). Net -3 tests / -71 assertions. Rollback: disable the module on the ShopOS dashboard. **Wave 8 epic complete.**
  - **8.3b — results-grid enforcement + debounce** ✅ shipped 1.21.2 (2026-06-22) — owner live-QA: the facet panel read N products but the grid showed *all* products. Cause: the live search grid is a ShopOS **ProductSlider** (current-query mode), which doesn't treat a search as an archive and falls back to source `all` (every product). 1.21.1 first tried a `the_posts` 99999 net but the widget reads its own `wc_get_products()` query (not the constrained main query), so the net nuked the grid to ~0 — reverted. 1.21.2 hooks the widget's own `shopos_core/product_slider/query_args` filter (`Results_Query::constrain_slider_query`, scoped to `source=current_query`, term read from `$wp_the_query` to survive Elementor's render swap) and injects the engine ids via `include`/`orderby=include`. Also lowered `Indexer::DEBOUNCE_DELAY` 30s→10s for near-live index freshness (owner request; still batched/background). Net +1 test.
  - **8.3c — request-term root-cause fix** ✅ shipped 1.21.3 (2026-06-22) — grid still showed all products + page-2 pagination. Real cause: the live search page is an **Elementor product-archive** template whose main query carries `post_type=product` but not the `s` query var — the term is only in the URL. So `apply()` (which gated on `is_search()` and read `$q->get('s')`) never engaged, while ShopFilters (reads the term from the request) was correct — hence the 3-facets-but-all-products mismatch. Fix: `apply()` reads the term from `$_GET['s']` (sanitised) and triggers on **product main query + request term + has_data** (no `is_search()` gate), constraining the main query so both the grid AND pagination (`max_num_pages`) follow the engine match count; `constrain_slider_query` reads the request term too. Net -1 test (`should_handle` lost its `is_search` param/case).

---

## Wave 9 — Product Page (new module — designed PDP + coupon notice + stock urgency)

Owner request (2026-07-06): a new module that overrides the current WooCommerce/Elementor single-product page with a fully designed, responsive PDP, and productizes two existing functions.php snippets (a coupon-discount price notice and a variation stock-scarcity badge) as toggles with owner-editable strings. Owner explicitly chose **one wave / one PR** (approved override of the 12-file per-PR ceiling), **Option A** (full PHP template takeover over an Elementor-layer restyle), and **coupon validation with auto-hide** (notice hidden when no live WC coupon with the configured code exists).

- **9.1 — ProductPage module** ✅ shipped 1.22.0 (#109, 2026-07-06) — new module `src/Modules/ProductPage/` (auto-discovered; disabled by default on existing installs until enabled on the ShopOS dashboard), three independent feature flags (default off — Hard Rule #1):
  - `shopos_core_product_page_coupon_notice_enabled` — `Coupon_Notice`: "enter coupon X and pay Y" notice. Settings `coupon_code` + `coupon_percent`; two Labels (`coupon_intro`/`coupon_outro`); renders on `woocommerce_single_product_summary` prio 25 **and** shortcode `[shopos_discounted_price]` (+ legacy alias `[discounted_price]` — the Elementor-built PDP never fires the summary stack, so the shortcode is the placement path until the layout flag is on). Improvements over the snippet: percent is a setting (no hardcoded `* 0.5`), notice auto-hides when the coupon is deleted/expired (`wc_get_coupon_id_by_code` + `get_date_expires`), and variable products get a per-variation `wc_price()` map that live-swaps on `found_variation`/`reset_data` (the snippet froze on the min price). Tax-display-correct via `wc_get_price_to_display()`.
  - `shopos_core_product_page_stock_urgency_enabled` — `Stock_Urgency`: variable-product scarcity badge (1 unit → "Last one in stock"; 2..`urgency_max` → "Only {count} left", both Labels; `urgency_max` setting, default 5). Renders on summary prio 35 + `[shopos_stock_urgency]` (+ legacy `[stock_urgency]`). Cleanups: enqueued assets instead of inline `<style>/<script>`, `wp_json_encode` data-attribute payload, warning-amber tokens per DESIGN.md's low-stock semantic (was red), **font-family stripped — inherits the page font** (owner request; the "Ploni" stack is gone). Variation reads use the light `get_available_variations('objects')` path (1.21.34 precedent).
  - `shopos_core_product_page_layout_enabled` — `Template_Loader` + `templates/single-product.php` (theme-overridable at `shopos/product_page/single-product.php`): full single-product takeover via `template_include` prio 9999 (wins over Elementor Pro's Theme Builder location), styled by `product-page.css` to DESIGN.md's "Quiet Boutique" system — token-driven (`--shopos-ui-*` with literal fallbacks), RTL-first logical properties, Hebrew flat tracking. Layout: breadcrumb → responsive two-column grid (native WC gallery with zoom/lightbox/thumbnail-slider — theme supports added by the module — | sticky summary column rendering the standard summary hook stack, so VariationSwatches / RestockNotify / structured data / both widgets above light up unaided) → accordion sections (`<details>` rendering the `woocommerce_product_tabs` stack; WC's tabs/upsells/related defaults detached at takeover, hook still fired for third parties) → restyled upsells/related grid → mobile sticky add-to-cart bar (IntersectionObserver reveal, scroll-back CTA, safe-area padding). Rollback: flag off → current Elementor page renders untouched.
  - New additive filters: `shopos_core/product_page/{show_coupon_notice, coupon_notice_html, show_stock_urgency, urgency_messages}`. New option literals `shopos_core_product_page_label_` + shortcode actions → `baseline-options-declared.txt` +3, `baseline-hooks.txt` +11 (all additive). Test-infra: `remove_action`/`locate_template`/`add_theme_support` bootstrap stubs. +44 reported tests / +168 reported assertions (`ProductPageModuleTest`, `ProductPageCouponNoticeTest`, `ProductPageStockUrgencyTest`, `ProductPageTemplateTest`; includes registry-iterating tests scaling with the new module + 3 flags). Hebrew wording ships via the Labels settings fields (§4.2 pattern), not the `.po`. Rollback: `wp option update shopos_core_product_page_<feature>_enabled 0` per feature, or disable the module on the dashboard.
  - Deferred live-QA (staging/a live store): Elementor-template precedence, gallery behaviours, VS buy box inside the new summary, coupon notice + urgency on a real variable product, sticky-bar UX, RTL mirror check.
- **9.1a — PDP design critique follow-up** ✅ shipped 1.22.1 (#109, 2026-07-06) — owner ran a design critique (impeccable, 27/40; snapshot in `.impeccable/critique/`) and chose "all of it": (a) **editorial gallery** — dropped the `wc-product-gallery-slider` theme support so the gallery renders as one stacked wrapper, laid out first-image-full-width + 2-up grid on desktop and a scroll-snap strip with a JS dot rail on mobile (no flexslider/thumbnail chrome — DESIGN.md's own anti-reference); zoom + lightbox kept, a `shopos-ui-img-swap` fade acknowledges variation image swaps; (b) **VariationSwatches reconciliation** — inside `.shopos-ui-pdp` the VS buy box adopts the ink system by remapping VS's own `--shopos-*` custom properties (declared on `.shopos-buy-box`, so a higher-specificity remap wins with zero VS edits) + scoped `!important` for the hardcoded pill radii and the sticky-bar red CTA; `.shopos-pdp-price` gets the standard price hierarchy slot; (c) **sticky-bar coexistence** — our mobile bar defers entirely when `.shopos-sticky-bar` is in the DOM (JS guard + `:has()` CSS belt) so two bars never stack, and its price now live-syncs on `found_variation`/`reset_data`; (d) **trust line** — two new Labels (`trust_shipping`/`trust_returns`, **empty default = hidden**; owner writes the wording in settings) render under the buy box via the pure `Template_Loader::trust_html()` with quiet truck/returns icons; (e) polish — accordion open fade/rise, `text-wrap: balance/pretty`, focus-visible rings, scoped `single_product_archive_thumbnail_size`→`large` around the upsell/related loops (1.21.18 lesson), all reduced-motion-gated. +3 reported tests, +12 reported assertions (`ProductPageTemplateTest` trust_html: hidden-until-written / filled-items-only / escaping). Baselines: position-only template line shifts. **766 tests / 2352 assertions.**
- **Flag-graduation sweep** ✅ shipped 1.23.0 (#111, 2026-07-06) — ProductPage's three flags (`coupon_notice` / `stock_urgency` / `layout`) plus flags across six other modules graduated to always-on and hard-removed from `Feature_Flags`; the module-enable toggle is the single kill-switch (owner-directed, same posture as Shop Filters 6.6 / Search 8.4). Landed as its own commit ahead of 9.2; its own stacked PR (#111). Net-negative test count (flag-gated cases removed): **754 tests / 2123 assertions**.
- **9.2 — PDP live-QA follow-up** ✅ shipped 1.23.1 (#112, 2026-07-06) — owner live-QA feedback on the designed page, one combined PR (owner-approved over the 12-file ceiling; no new feature flags — polish/bugfix on the always-on module + one additive setting):
  - **Gallery blank-after-swap fix (root cause)** — the theme (and Elementor Pro) declare `wc-product-gallery-slider`, so flexslider actually booted at runtime and fought the editorial `display:grid` (the 9.1a "deliberately skips the slider" premise was false). `Template_Loader::add_gallery_supports()` now `remove_theme_support('wc-product-gallery-slider')` at priority 99 (after late adders); the wrapper stays a plain stacked container.
  - **Lightbox → in-image zoom** — `remove_theme_support('wc-product-gallery-lightbox')` (keeps `wc-product-gallery-zoom` hover-magnify); product-page.js blocks the gallery `<a>` raw-image navigation that WC's PhotoSwipe init used to preventDefault.
  - **VS buy box reverted to native** — dropped the 9.1a `--shopos-*` ink remap + `!important` radius/sticky overrides (owner prefers VS's own red 999px pills). New **`button_color`** color setting injects `--shopos-primary` (+ the hardcoded sticky-bar red) inline via `Template_Loader::button_color_css()` (pure, unit-tested); empty = VS's native red.
  - **Price forced ink** — the VS `.shopos-pdp-price` reconciliation now uses `!important` + an explicit `__value` rule so the price reads ink instead of inheriting the theme's `.price{color}`.
  - **Coupon-price inline** — the intro+code+outro wrap in `.shopos-ui-coupon-notice__sentence`; a baseline flex row pins the price beside the sentence instead of dropping it to its own line.
  - **Trust + Additional information under the buy box** — rendered on `woocommerce_single_product_summary` at priority 36 / 38 (above meta 40), takeover-scoped via `Template_Loader::is_takeover()`; `additional_information` is unset from the accordion to avoid a duplicate.
  - **Subtle summary panel + 1rem radii** — `.shopos-ui-pdp__summary-col` gets a paper-dim panel + 1rem radius; gallery images go 12px → 1rem; gallery gaps split into row (16) / column (8).
  - **Mobile** — the dot rail is replaced by a scroll-progress bar (RTL-safe, scroll-driven); the fixed bar now reserves body space via `body.shopos-ui-pdp-sticky-active`; strip snaps to `start`.
  - **Related grid** — added the render-scoped `wp_calculate_image_sizes` companion to the `large` request (1.21.23 lesson) so cards stop pulling the viewport-wide srcset candidate; `contain` treatment kept. (The express-checkout/Google-Pay "buy now" item was an owner-side Stripe express-checkout setting — no code change.)
  - +3 reported tests, +12 reported assertions (`ProductPageTemplateTest`: `button_color_css` matrix + gallery-supports reconciliation). New `remove_theme_support` bootstrap stub. `baseline-hooks.txt` regenerated (position-only shifts + the additive `woocommerce_product_additional_information_heading` filter). **757 tests / 2135 assertions.** Rollback: `git revert` the release, or clear `shopos_core_product_page_button_color` for the buy-colour part.
  - Deferred live-QA (the live store): flexslider actually gone (no `.flex-viewport`), zoom-on-touch, coupon inline in the narrow Hebrew column, trust/addl-info order under the buy box, mobile progress bar + sticky-space, RTL mirror.
- **9.3 — PDP live-QA round 2 + perf pass** ✅ shipped 1.24.0 (#114, 2026-07-06) — the owner's second live round (10 items incl. "is the plugin making the site slower?"), one combined PR (owner-approved bundling, the 9.2 shape; no new flags — bugfix/polish on the always-on module):
  - **Gallery image-size root cause** — removing the slider support (9.2) made WC's `wc_get_gallery_image_html()` silently drop every non-main gallery image from `woocommerce_single` to the ~100px `gallery_thumbnail` (verified against WC source), which stretched blurry — the takeover now adds a `woocommerce_gallery_image_size` → `woocommerce_single` filter (scoped, added in `maybe_takeover()`).
  - **Gallery layout** (final after owner follow-ups 1.24.1–1.24.3) — one swipeable image at a time at **every** breakpoint: a scroll-snap flex carousel with the JS scroll-progress bar below it (floored at a ~14% resting fill so it reads as an indicator before scrolling). The desktop thumbnail row was tried (1.24.1/1.24.2) then dropped (1.24.3, owner request) in favour of the same carousel as mobile; desktop, which has no touch swipe, gets mouse **click-drag-to-scroll** (grab cursor; `.is-grabbing` suspends scroll-snap during the drag, native image-drag suppressed). **No lightbox**: a capture-phase click handler blocks any lightbox (WC PhotoSwipe / theme / Elementor) and the raw `<a href>` navigation before it fires, and WC's expand trigger is `display:none` — a click opens nothing. All slots render at `woocommerce_single` size (the `woocommerce_gallery_image_size` filter), so every slide is sharp.
  - **Click-to-swap** (desktop 2-up only) — clicking a secondary gallery image trades `src`/`srcset`/link/data attrs with the main slot (attributes move, nodes and classes stay, so `.wp-post-image` remains on slot 1 and WC's native variation image update keeps targeting it); hover-zoom re-inits via `woocommerce_gallery_init_zoom`. Variation-image swap itself needs no new code — the VS buy box drives WC's native `variations_form` (verified), live-QA confirms.
  - **Related grid 3+1 + mobile fix** — WC core's `.woocommerce ul.products[class*="columns-"] li.product` (0,4,2) outranked the scoped grid, leaking floats/percentage widths/`.first{clear}`, and WC's clearfix `::before/::after` render as anonymous grid items that shove the 4th card onto its own row; the grid now mirrors the attribute selector, pins the killers with `!important`, suppresses the pseudos, and the template pins `woocommerce_output_related_products_args` to posts 4 / columns 4.
  - **Additional information = collapsed `<details>`** — the under-buy-box block is a chevroned, collapsed-by-default `<details>` (owner: "a tab, not just open") built by the pure `Template_Loader::additional_information_html()`; the 9.2 `<h2>` title — which an Elementor-kit `h2` rule (0,1,1) outranked, rendering it kit-huge — is now a `<span>` under double-class selectors, and the attributes-table typography is pinned to `--shopos-ui-text-sm`.
  - **Coupon notice** — summary priority 25 → **31** so it always renders directly under the buy box/swatches (it sat above them); background flipped to a white card + hairline border (its paper-dim bg had become invisible against the 9.2 paper-dim summary panel).
  - **Buy-box full-width pin** — `.shopos-ui-pdp` defensively pins `form.cart` / `.shopos-buy-box` (+ actions rows) to `float:none / width:100% / max-width:none !important`: nothing in this repo or VS sets the reported ~50%-left layout, so it's a site-level rule from the old Elementor PDP era — the early-loaded pin defeats it from first paint (also kills the right→left flash) and lifts VS's 640px cap; the standard-form flex rule gains `:not(.shopos-buy-box)` so VS's simple buy box keeps its own column layout. Hunt-and-remove of the offending a live store rule stays a live task.
  - **Title full width** — dropped `text-wrap: balance` (it equalises line lengths, so multi-line titles refused to fill the column).
  - **Perf ("way slower?" audit + first fix)** — ranked audit: (1) Coupon_Notice + Stock_Urgency each ran an independent full `get_available_variations('objects')` per variable-PDP view — and the coupon map adds per-variation `wc_get_price_to_display()` tax calcs — new since Wave 9; (2) QuickView ships 4 CSS/JS handles on 100% of front-end pages (pre-existing gating, newly always-on); (3) verify CheapestDefaultVariation `pdp_only` is still ON live (off = catastrophic per-archive-card sweeps). Fixed here: new shared per-request `Variations` memo collapses the two widget sweeps to one enumeration per product. QuickView asset gating + the pdp_only check + before/after TTFB measurement = follow-up.
  - +9 reported tests, +23 reported assertions (`ProductPageVariationsTest` memo matrix incl. id-keyed fresh-object hit; `ProductPageTemplateTest` gallery-size filter on/off-product + `<details>` markup/escaping; `ProductPageCouponNoticeTest` summary-priority-31). `baseline-hooks.txt` position-only regen. **765 tests / 2158 assertions.** Rollback: disable the Product Page module, or `git revert` the release.
  - Deferred live-QA (the live store): gallery sharpness (non-main images now `woocommerce_single`) + stacked mobile order; related grid 4-up desktop / 2-up mobile; click-to-swap + zoom re-init; variation-image swap into the main slot; coupon under the buy box as a visible card; buy box full-width with no paint flash (and remove the offending site-level rule at source); addl-info collapsed + small; multi-line title fills the column; PDP TTFB before/after on a many-variation product.

---

## Bug-proofing techniques (apply to every PR)

- **Snapshot before/after**: capture module output before first commit. After changes, diff with flag OFF — must be empty.
- **Local testing first**: every Wave-1 and Wave-2 PR runs on a separate local WP install with real product data before merge.
- **Rollback drill**: for each P0 PR, document and test the exact `wp option update` command on the local install.
- **Conflict detection**: re-run the legacy-plugin conflict check (`Dashboard.php:97`) after each Wave-2 merge. New code must not trigger false positives.

---

## What is NOT on the roadmap

Items dropped per `/docs/decisions-2026-04-28.md`:

- ~~Roadmap #7~~ — theme.json unification (Elementor-only, §4.3)
- ~~Roadmap #8~~ — REST API surface (no headless need, §4.4)
- ~~Roadmap #13~~ — Wishlist module (internal-only, §4.1)
- ~~Roadmap #14~~ — Quick-View module — **re-opened 2026-06-11** by owner request (revisit trigger §6); now Wave 7
- ~~Roadmap #15~~ — Critical CSS / WebP (P3, only if a client asks; can use WP Rocket meanwhile)
- ~~A25~~ — Telemetry (skipped, §4.8)
- ~~D9~~ — Frontend asset URL filter (no white-labeling, §4.7)

Revisit these only if the strategic decisions change.
